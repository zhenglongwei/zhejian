<view class="image-grid image-grid--col-{{columns}}">
  <image
    wx:for="{{displayImages}}"
    wx:key="*this"
    class="image-grid__cell"
    src="{{item}}"
    mode="aspectFill"
    data-index="{{index}}"
    bindtap="onPreview"
  />
</view>
