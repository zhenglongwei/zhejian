<view class="faq-list faq-list--{{variant}}">
  <view
    wx:for="{{items}}"
    wx:key="q"
    class="faq-list__item"
  >
    <text class="faq-list__q text-body">{{showQPrefix ? 'Q：' : ''}}{{item.q}}</text>
    <text class="faq-list__a text-caption">{{item.a}}</text>
  </view>
</view>
