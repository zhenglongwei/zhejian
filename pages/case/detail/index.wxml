<view class="page case-detail">
  <block wx:if="{{status === 'loading'}}">
    <view class="page-content case-detail__pad">
      <ui-skeleton rows="{{6}}" title="{{true}}" />
    </view>
  </block>

  <block wx:elif="{{status === 'error'}}">
    <view class="page-content">
      <ui-empty
        title="无法加载案例"
        description="{{errorMessage}}"
        actionText="重试"
        bind:action="onRetry"
      />
    </view>
  </block>

  <block wx:elif="{{detail}}">
    <view class="case-detail__body page-content">
      <view class="case-detail__tags">
        <ui-tag
          wx:for="{{tagList}}"
          wx:key="text"
          variant="{{item.variant}}"
          text="{{item.text}}"
        />
      </view>
      <view class="case-detail__privacy">
        <ui-privacy-banner scene="share" />
      </view>
      <text class="case-detail__title text-h1">{{detail.title}}</text>
      <ui-ai-summary-block content="{{detail.aiSummary}}" />
      <ui-key-info-table rows="{{detail.keyInfo}}" />
      <view class="case-detail__section">
        <text class="case-detail__section-title text-h3">价格说明</text>
        <ui-price-display
          mode="{{detail.priceMode}}"
          minAmount="{{detail.minAmount}}"
          maxAmount="{{detail.maxAmount}}"
        />
        <view wx:if="{{isHistory}}" class="case-detail__notice">
          <ui-compliance-notice type="history" />
        </view>
        <view class="case-detail__notice">
          <ui-compliance-notice type="casePrice" />
        </view>
      </view>
      <view wx:if="{{detail.faultDesc}}" class="case-detail__section">
        <text class="case-detail__section-title text-h3">故障表现</text>
        <text class="text-body">{{detail.faultDesc}}</text>
      </view>
      <view wx:if="{{detail.inspectResult}}" class="case-detail__section">
        <text class="case-detail__section-title text-h3">检查结果</text>
        <text class="text-body">{{detail.inspectResult}}</text>
      </view>
      <view wx:if="{{detail.repairPlan}}" class="case-detail__section">
        <text class="case-detail__section-title text-h3">维修方案</text>
        <text class="text-body">{{detail.repairPlan}}</text>
      </view>
      <view wx:if="{{detail.priceFactors.length}}" class="case-detail__section">
        <text class="case-detail__section-title text-h3">影响价格的因素</text>
        <view class="case-detail__factors">
          <text
            wx:for="{{detail.priceFactors}}"
            wx:key="*this"
            class="case-detail__factor text-caption"
          >· {{item}}</text>
        </view>
      </view>
      <view class="case-detail__section">
        <text class="case-detail__section-title text-h3">维修过程</text>
        <ui-album-node
          wx:for="{{detail.nodes}}"
          wx:key="id"
          title="{{item.title}}"
          images="{{item.images}}"
          note="{{item.note}}"
        />
      </view>
      <view wx:if="{{linkedReviews.length}}" class="case-detail__section">
        <ui-review-list-section
          sectionTitle="关联评价"
          titleSize="h3"
          hint="来自本订单用户的真实服务评价（已审核），为用户主观体验，不代表平台结论。"
          reviews="{{linkedReviews}}"
          status="normal"
          topTags="{{[]}}"
        />
      </view>
      <view class="case-detail__section">
        <text class="case-detail__section-title text-h3">关联门店</text>
        <view class="case-detail__store" bindtap="onStoreTap">
          <text class="text-body case-detail__store-name">{{detail.storeName}}</text>
          <text class="text-caption case-detail__city">{{detail.city}}</text>
          <text class="text-caption text-link case-detail__store-link">查看门店详情 ›</text>
        </view>
      </view>
      <view wx:if="{{faqList.length}}" class="case-detail__section">
        <text class="case-detail__section-title text-h3">常见问题</text>
        <view
          wx:for="{{faqList}}"
          wx:key="q"
          class="case-detail__faq-item"
        >
          <text class="case-detail__faq-q text-body">{{item.q}}</text>
          <text class="case-detail__faq-a text-caption">{{item.a}}</text>
        </view>
      </view>
      <view wx:if="{{relatedCases.length}}" class="case-detail__section">
        <text class="case-detail__section-title text-h3">相关案例</text>
        <view class="case-detail__related">
          <ui-case-card
            wx:for="{{relatedCases}}"
            wx:key="id"
            caseId="{{item.id}}"
            caseSource="{{item.source}}"
            coverImage="{{item.coverImage}}"
            title="{{item.title}}"
            serviceName="{{item.serviceName}}"
            summary="{{item.summary}}"
            priceMode="{{item.priceMode}}"
            minAmount="{{item.minAmount}}"
            maxAmount="{{item.maxAmount}}"
            storeName="{{item.storeName}}"
            viewCount="{{item.viewCount}}"
            bind:tap="onRelatedCaseTap"
          />
        </view>
      </view>
    </view>
    <ui-fixed-bottom-bar>
      <ui-button type="secondary" size="sm" slot="left" bind:tap="onShare">分享</ui-button>
      <ui-button type="primary" slot="right" bind:tap="onBook">预约到店</ui-button>
    </ui-fixed-bottom-bar>
  </block>
</view>
