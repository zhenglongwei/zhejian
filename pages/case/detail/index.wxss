@import '../../../styles/deep-link-detail.wxss';

.case-detail__tags {
  margin-bottom: var(--space-sm);
}

.case-detail__title {
  display: block;
  margin-bottom: var(--space-md);
}

.case-detail__evidence {
  margin-top: var(--space-md);
}

.case-detail__phone-card {
  margin-top: var(--space-sm);
}
