.album-authorize__list {
  display: flex;
  flex-direction: column;
  gap: var(--space-md);
  padding-top: var(--space-sm);
  padding-bottom: var(--space-xl);
}

.album-authorize__state {
  padding-top: var(--space-lg);
  padding-bottom: var(--space-xl);
}
