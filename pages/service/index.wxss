.service-page__intro {
  padding-top: var(--space-md);
}

.service-page__desc {
  display: block;
  margin-top: var(--space-xs);
  color: var(--color-text-secondary);
}

.service-page__compliance {
  display: block;
  margin-top: var(--space-md);
}

.service-page__tabs {
  margin-top: var(--space-md);
}

.service-page__list {
  padding-top: var(--space-md);
  padding-bottom: var(--space-lg);
  display: flex;
  flex-direction: column;
  gap: var(--space-md);
}
