.mine__pad {
  padding-top: var(--space-md);
}

.mine__sk-block {
  margin: var(--space-md) 0;
}

.mine__body {
  padding-top: var(--space-md);
  padding-bottom: var(--space-xl);
  display: flex;
  flex-direction: column;
  gap: var(--space-md);
}
