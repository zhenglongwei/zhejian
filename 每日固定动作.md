每天开发可以固定节奏：

> **卷二（商家工作台）**：见 [`卷二开发每日固定动作.md`](./卷二开发每日固定动作.md) — **当前 Sprint**  
> **卷三（ops / 脱敏引擎）**：见 [`卷三开发每日固定动作.md`](./卷三开发每日固定动作.md)（P0 已收口，发版前回归）

1. **开做前**：Read 对应 PRD + 设计体系相关章节；**商家工作台**（`packageMerchant/`，不含相册）用 **`merchant-workbench-scaffold`** 先出计划；新页面用 `page-scaffold` 先出计划；**运营 Web 审核台**（`admin-web/`）用 `ops-admin-scaffold` 先出计划。
2. **写完一块**：`design-system-check` +（有价格则）`price-compliance-check` +（有相册/审核则）`privacy-desensitization-check` +（商家/mock 替换则）`api-integration`。
3. **合并前**：`component-usage-review`；（若改 `packageMerchant/` 商家页）`merchant-workbench-scaffold` 自查；（若改 `admin-web/`）`ops-admin-scaffold` 自查。
4. **发体验版前**：`release-checklist`。

