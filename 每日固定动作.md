每天开发可以固定节奏：

> **卷三（ops / 脱敏引擎）**：见 [`卷三开发每日固定动作.md`](./卷三开发每日固定动作.md)

1. **开做前**：Read 对应 PRD + 设计体系相关章节；新页面用 `page-scaffold` 先出计划；**运营 Web 审核台**（`admin-web/`）用 `ops-admin-scaffold` 先出计划。
2. **写完一块**：`design-system-check` +（有价格则）`price-compliance-check` +（有相册/审核则）`privacy-desensitization-check`。
3. **合并前**：`component-usage-review`；（若改 `admin-web/`）`ops-admin-scaffold` 自查。
4. **发体验版前**：`release-checklist`。

