<view class="page merchant-album-page merchant-album-edit">
  <block wx:if="{{status === 'loading'}}">
    <view class="page-content merchant-album-edit__pad">
      <ui-skeleton rows="{{3}}" title="{{true}}" />
      <ui-skeleton rows="{{6}}" />
    </view>
  </block>

  <block wx:elif="{{status === 'error'}}">
    <view class="page-content">
      <ui-empty title="无法加载服务相册" description="{{errorMessage}}" actionText="重试" bind:action="onRetry" />
    </view>
  </block>

  <block wx:elif="{{detail}}">
    <view class="merchant-album-edit__shell">
      <scroll-view scroll-y class="merchant-album-edit__scroll" show-scrollbar="{{false}}">
        <view class="page-content merchant-album-edit__body">
      <ui-card shadow="{{true}}" padding="{{true}}">
        <view wx:if="{{lockHint}}" class="merchant-album-edit__lock-banner">
          <text class="text-caption">{{lockHint}}</text>
        </view>
        <view class="merchant-album-edit__head">
          <view class="merchant-album-edit__head-main">
            <text class="text-h3">{{detail.serviceName}}</text>
            <view class="merchant-album-edit__head-tags">
              <ui-tag variant="{{statusVariant}}" text="{{statusLabel}}" />
              <ui-tag wx:if="{{readOnly}}" variant="info" text="仅查看" />
              <ui-tag wx:if="{{templateName}}" variant="info" text="{{templateName}}" />
            </view>
          </view>
          <view
            wx:if="{{canSwitchTemplate && templateOptions.length}}"
            class="merchant-album-edit__template-switch"
          >
            <picker
              mode="selector"
              range="{{templateOptions}}"
              range-key="name"
              value="{{templatePickerIndex}}"
              disabled="{{readOnly || switching || saving || completing}}"
              bindchange="onTemplateChange"
            >
              <view class="merchant-album-edit__template-switch-action">
                <text class="merchant-album-edit__template-switch-text text-caption">切换模板</text>
              </view>
            </picker>
            <view class="merchant-album-edit__template-switch-divider" aria-hidden="true" />
            <view
              class="merchant-album-edit__template-switch-help"
              aria-role="button"
              aria-label="切换模板说明"
              catchtap="onTemplateSwitchHelp"
            >
              <text class="merchant-album-edit__template-switch-help-icon">?</text>
            </view>
          </view>
        </view>

        <view class="merchant-album-edit__meta">
          <view class="merchant-album-edit__meta-compact-bar" bindtap="onToggleVehicleMeta">
            <view class="merchant-album-edit__meta-compact-main">
              <text class="text-body merchant-album-edit__meta-compact-line">
                {{vehiclePlate || '未填车牌'}} · {{vehicleCompactModel || '未填车型'}}
              </text>
              <text class="text-caption text-secondary merchant-album-edit__meta-compact-vin">
                车架号 {{vehicleVin || '未填'}}
              </text>
            </view>
            <text class="text-caption text-link">{{vehicleMetaExpanded ? '收起' : '展开'}}</text>
          </view>

          <block wx:if="{{vehicleMetaExpanded}}">
          <view class="merchant-album-edit__meta-row">
            <text class="merchant-album-edit__meta-label text-caption">车型</text>
            <view class="merchant-album-edit__meta-fields">
              <input
                class="merchant-album-edit__meta-input"
                placeholder="品牌 *"
                value="{{vehicleBrand}}"
                disabled="{{readOnly || saving || completing || switching}}"
                data-field="vehicleBrand"
                bindinput="onVehicleInput"
              />
              <input
                class="merchant-album-edit__meta-input"
                placeholder="车系 *"
                value="{{vehicleSeries}}"
                disabled="{{readOnly || saving || completing || switching}}"
                data-field="vehicleSeries"
                bindinput="onVehicleInput"
              />
            </view>
          </view>

          <view class="merchant-album-edit__meta-row">
            <text class="merchant-album-edit__meta-label text-caption">年款</text>
            <view class="merchant-album-edit__meta-fields">
              <input
                class="merchant-album-edit__meta-input"
                placeholder="年款（选填）"
                value="{{vehicleModelYear}}"
                disabled="{{readOnly || saving || completing || switching}}"
                data-field="vehicleModelYear"
                bindinput="onVehicleInput"
              />
            </view>
          </view>

          <view class="merchant-album-edit__meta-row">
            <text class="merchant-album-edit__meta-label text-caption">车牌</text>
            <view class="merchant-album-edit__meta-fields">
              <input
                class="merchant-album-edit__meta-input"
                placeholder="车牌号"
                value="{{vehiclePlate}}"
                maxlength="12"
                disabled="{{readOnly || saving || completing || switching}}"
                data-field="vehiclePlate"
                bindinput="onVehicleInput"
              />
            </view>
          </view>

          <view class="merchant-album-edit__meta-row">
            <text class="merchant-album-edit__meta-label text-caption">车架号</text>
            <view class="merchant-album-edit__meta-fields merchant-album-edit__meta-fields--vin">
              <input
                class="merchant-album-edit__meta-input merchant-album-edit__meta-input--vin"
                placeholder="17 位 VIN"
                value="{{vehicleVin}}"
                maxlength="17"
                disabled="{{readOnly || saving || completing || switching || vinDecoding}}"
                data-field="vehicleVin"
                bindinput="onVehicleInput"
              />
              <text
                wx:if="{{!readOnly}}"
                class="text-link text-caption merchant-album-edit__vin-decode {{vinDecoding ? 'is-loading' : ''}}"
                bindtap="onDecodeVin"
              >{{vinDecoding ? '解析中…' : '解析车型'}}</text>
            </view>
          </view>

          <view class="merchant-album-edit__meta-row">
            <text class="merchant-album-edit__meta-label text-caption">发动机</text>
            <view class="merchant-album-edit__meta-fields">
              <input
                class="merchant-album-edit__meta-input"
                placeholder="发动机型号（选填）"
                value="{{vehicleEngineModel}}"
                disabled="{{readOnly || saving || completing || switching}}"
                data-field="vehicleEngineModel"
                bindinput="onVehicleInput"
              />
            </view>
          </view>

          <view class="merchant-album-edit__meta-row">
            <text class="merchant-album-edit__meta-label text-caption">底盘</text>
            <view class="merchant-album-edit__meta-fields">
              <input
                class="merchant-album-edit__meta-input"
                placeholder="底盘型号（选填）"
                value="{{vehicleChassisCode}}"
                disabled="{{readOnly || saving || completing || switching}}"
                data-field="vehicleChassisCode"
                bindinput="onVehicleInput"
              />
            </view>
          </view>
          <text class="text-caption text-secondary merchant-album-edit__vin-tip">里程请在接车节点拍摄仪表并写在本图说明；解析失败可手工改品牌/车系。</text>

          <view id="merchant-album-owner-phone" class="merchant-album-edit__meta-row">
            <text class="merchant-album-edit__meta-label text-caption">车主</text>
            <view class="merchant-album-edit__meta-body">
              <block wx:if="{{readOnly}}">
                <text
                  wx:if="{{hasOwner && detail.userPhoneDisplay}}"
                  class="text-body merchant-album-edit__owner-value"
                >{{detail.userPhoneDisplay}}</text>
                <text wx:else class="text-caption text-secondary">未关联车主</text>
              </block>
              <block wx:else>
                <text
                  class="text-link text-body merchant-album-edit__owner-link"
                  bindtap="onInviteOwnerScan"
                >请车主扫码关联</text>
                <input
                  class="merchant-album-edit__meta-input merchant-album-edit__owner-input"
                  type="number"
                  maxlength="11"
                  placeholder="填写车主手机号"
                  value="{{ownerPhoneInput}}"
                  disabled="{{saving || completing || switching}}"
                  bindinput="onOwnerPhoneInput"
                />
                <text class="text-caption merchant-album-edit__owner-test-tip">保存后可随时修改；完工前须保留有效手机号</text>
              </block>
            </view>
          </view>
          </block>
        </view>
      </ui-card>

      <ui-card
        wx:if="{{showCaseDraftEntry}}"
        shadow="{{true}}"
        padding="{{true}}"
        class="merchant-album-edit__optimize-card"
      >
        <view class="merchant-album-edit__optimize-head">
          <text class="text-h3">公开案例稿</text>
          <ui-tag variant="info" text="{{readOnly ? '仅查看' : '可修订'}}" />
        </view>
        <block wx:if="{{caseDraftHasPreview}}">
          <text wx:if="{{caseDraftPreviewTitle}}" class="text-body merchant-album-edit__draft-title">{{caseDraftPreviewTitle}}</text>
          <text wx:if="{{caseDraftPreviewSummary}}" class="text-caption text-secondary merchant-album-edit__draft-summary">{{caseDraftPreviewSummary}}</text>
          <view class="merchant-album-edit__draft-actions">
            <text class="text-caption text-link" bindtap="onOpenCaseDraft">查看全文</text>
            <text
              wx:if="{{caseDraftConfirmed}}"
              class="text-caption text-link merchant-album-edit__draft-action-gap"
              bindtap="onCopyCaseDraftExport"
            >复制发自媒体</text>
          </view>
        </block>
        <block wx:else>
          <text class="text-caption text-secondary merchant-album-edit__optimize-hint">暂无案例摘要</text>
          <text class="text-caption text-link" bindtap="onOpenCaseDraft">查看全文</text>
        </block>
      </ui-card>

      <view class="merchant-album-edit__tabs">
        <ui-tabs
          tabs="{{stageTabs}}"
          activeKey="{{stageTabs[stageIndex].key}}"
          scrollable="{{true}}"
          bind:change="onStageTabChange"
        />
      </view>

      <view wx:if="{{commonShootAvoidTags.length}}" class="merchant-album-edit__shoot-common">
        <text class="text-caption merchant-album-edit__shoot-common-label">拍摄注意</text>
        <view class="merchant-album-edit__shoot-common-tags">
          <text
            wx:for="{{commonShootAvoidTags}}"
            wx:key="*this"
            class="merchant-album-edit__shoot-common-tag text-caption"
          >{{item}}</text>
        </view>
      </view>

      <ui-card shadow="{{true}}" padding="{{true}}">
        <block wx:if="{{showStageChecklist}}">
          <album-checklist-editor
            mode="stage"
            title="{{checklistStageTitle || checklistCategoryLabel || '检查项目'}}"
            hint="{{checklistStageHint}}"
            items="{{stageChecklistItems}}"
            completeness="{{checklistCompleteness}}"
            stageId="{{stages[stageIndex].id}}"
            focusItemKey="{{focusChecklistItemKey}}"
            readOnly="{{readOnly}}"
            bind:itemimageschange="onChecklistItemImagesChange"
            bind:itemoutcome="onChecklistOutcomeChange"
            bind:aicompare="onChecklistAiCompare"
          />
        </block>

        <block wx:if="{{showWorkQueue}}">
          <album-checklist-editor
            mode="work"
            title="施工清单"
            hint="由检测延伸的需处理项：拍照留证并点选结论；补充说明选填。若本次不做，点「删除」后再选是否跟进。"
            items="{{workQueueItems}}"
            stageId="{{stages[stageIndex].id}}"
            focusItemKey="{{focusChecklistItemKey}}"
            readOnly="{{readOnly}}"
            bind:itemimageschange="onChecklistItemImagesChange"
            bind:itemoutcome="onChecklistOutcomeChange"
            bind:removework="onChecklistRemoveWork"
            bind:aicompare="onChecklistAiCompare"
          />
        </block>

        <block wx:if="{{showFollowUpList && followUpItems.length}}">
          <view class="merchant-album-edit__followup-entry">
            <view
              class="merchant-album-edit__followup-bar"
              bindtap="onToggleFollowUpExpanded"
            >
              <view class="merchant-album-edit__followup-bar-main">
                <text class="text-body">跟进 {{followUpItems.length}} 项</text>
                <text class="text-caption text-secondary">回访备忘 · 非本单过程记录</text>
              </view>
              <text class="text-caption text-link">{{followUpExpanded ? '收起' : '查看'}}</text>
            </view>
            <block wx:if="{{followUpExpanded}}">
              <album-checklist-editor
                mode="followup"
                title=""
                hint="择日再约项；可重新纳入施工清单。"
                items="{{followUpItems}}"
                readOnly="{{readOnly}}"
                bind:restorework="onChecklistRestoreWork"
              />
            </block>
          </view>
        </block>

        <block wx:if="{{isComparePairStage}}">
          <album-evidence-slots
            wx:if="{{showStageEvidenceSlots}}"
            items="{{stageEvidenceSlots}}"
            disabled="{{readOnly || saving || completing || switching}}"
            uploadHint="{{nodes[stageIndex].publicUploadHint || uploadPrivacyHint}}"
            bind:change="onEvidenceSlotsChange"
          />
          <album-compare-pair-editor
            title="{{nodes[stageIndex].title}}"
            description="{{nodes[stageIndex].description}}"
            photoTips=""
            compareHint="{{nodes[stageIndex].compareGuidance}}"
            requiredLevelLabel="{{nodes[stageIndex].requiredLevelLabel}}"
            requiredLevelVariant="{{nodes[stageIndex].requiredLevelVariant}}"
            uploadHint="{{nodes[stageIndex].publicUploadHint || uploadPrivacyHint}}"
            pairRows="{{comparePairRows}}"
            disabled="{{readOnly || saving || completing || switching}}"
            bind:rowschange="onCompareRowsChange"
            bind:syncfromassessment="onSyncCompareFromAssessment"
          />
        </block>

        <block wx:elif="{{isPartsStage || isPlanStage}}">
          <view class="merchant-album-edit__parts-node">
            <text class="text-h3">历史节点</text>
            <text class="text-caption text-secondary">报价单 OCR 与配件类型录入已下线。请到「检测记录」标记结果，在「施工过程」的待处理项目中挂图说明。</text>
          </view>
        </block>
        <block wx:elif="{{showStageChecklist || showWorkQueue}}">
          <album-evidence-slots
            wx:if="{{showStageEvidenceSlots}}"
            items="{{stageEvidenceSlots}}"
            disabled="{{readOnly || saving || completing || switching}}"
            uploadHint="{{nodes[stageIndex].publicUploadHint || uploadPrivacyHint}}"
            bind:change="onEvidenceSlotsChange"
          />
          <text class="text-caption text-secondary merchant-album-edit__other-photos-hint">下方「其他过程图」仅用于未挂到检查项的零散照片（可选）。</text>
          <ui-album-node
            wx:if="{{showStageProcessUploader}}"
            mode="{{readOnly ? 'view' : 'edit'}}"
            title="其他过程图"
            description=""
            photoTips=""
            compact="{{true}}"
            images="{{nodes[stageIndex].otherImages || []}}"
            data-index="{{stageIndex}}"
            data-other="1"
            bind:imageschange="onOtherNodeImages"
          />
        </block>

        <block wx:else>
          <album-evidence-slots
            wx:if="{{showStageEvidenceSlots}}"
            items="{{stageEvidenceSlots}}"
            disabled="{{readOnly || saving || completing || switching}}"
            uploadHint="{{nodes[stageIndex].publicUploadHint || planStageUploadHint}}"
            bind:change="onEvidenceSlotsChange"
          />
          <album-old-part-traces
            wx:if="{{showOldPartTraces}}"
            traces="{{oldPartTraces}}"
            partOptions="{{oldPartPartOptions}}"
            introHint="{{oldPartIntroHint}}"
            disabled="{{readOnly || saving || completing || switching}}"
            uploadHint="{{nodes[stageIndex].publicUploadHint || uploadPrivacyHint}}"
            bind:change="onOldPartTracesChange"
          />
            <ui-album-node
            wx:if="{{showStageProcessUploader}}"
            mode="{{readOnly ? 'view' : 'edit'}}"
            title="{{nodes[stageIndex].title}}"
            description="{{nodes[stageIndex].description}}"
            photoTips=""
            compareGuidance="{{nodes[stageIndex].compareGuidance}}"
            requiredLevelLabel="{{nodes[stageIndex].requiredLevelLabel}}"
            requiredLevelVariant="{{nodes[stageIndex].requiredLevelVariant}}"
            captionPlaceholder="{{nodes[stageIndex].captionPlaceholder}}"
            images="{{nodes[stageIndex].images}}"
            stampChecklistItemKey=""
            uploadHint="{{nodes[stageIndex].publicUploadHint || uploadPrivacyHint}}"
            data-index="{{stageIndex}}"
            bind:imageschange="onNodeImages"
          />
          <ui-album-node
            wx:elif="{{!showStageEvidenceSlots}}"
            mode="{{readOnly ? 'view' : 'edit'}}"
            title="{{nodes[stageIndex].title}}"
            description="{{nodes[stageIndex].description}}"
            photoTips=""
            compareGuidance="{{nodes[stageIndex].compareGuidance}}"
            requiredLevelLabel="{{nodes[stageIndex].requiredLevelLabel}}"
            requiredLevelVariant="{{nodes[stageIndex].requiredLevelVariant}}"
            captionPlaceholder="{{nodes[stageIndex].captionPlaceholder}}"
            images="{{nodes[stageIndex].images}}"
            stampChecklistItemKey=""
            uploadHint="{{nodes[stageIndex].publicUploadHint || planStageUploadHint}}"
            data-index="{{stageIndex}}"
            bind:imageschange="onNodeImages"
          />
          <view wx:if="{{isPlanStage}}" class="merchant-album-edit__extra-sop">
            <text class="text-caption merchant-album-edit__extra-sop-text">{{extraPartSopStage3Hint}}</text>
          </view>
        </block>

        <block wx:if="{{stages[stageIndex].id === 'stage_6'}}">
          <view class="merchant-album-edit__warranty">
            <text class="text-caption merchant-album-edit__plan-hint">{{warrantyIntro}}</text>
            <text class="merchant-album-edit__warranty-field-label text-caption">{{warrantyNoteLabel}}</text>
            <textarea
              class="merchant-form__textarea merchant-album-edit__warranty-textarea"
              maxlength="500"
              placeholder="{{warrantyNotePlaceholder}}"
              value="{{warrantyNote}}"
              disabled="{{readOnly || saving || completing || switching}}"
              bindinput="onWarrantyFieldInput"
              auto-height
            />
          </view>
        </block>
      </ui-card>
        <ui-card shadow="{{true}}" padding="{{true}}" class="merchant-album-edit__host-card">
        <text class="text-h3">案例托管</text>
        <text class="text-caption text-secondary">托管到辙见案例站后，可再决定是否公开。公开须脱敏并勾选真实性承诺。</text>
        <view class="merchant-album-edit__host-row">
          <ui-tag variant="{{hosted ? 'order' : 'default'}}" text="{{hosted ? (hostVisibility === 'public' ? '已托管·公开' : '已托管·私密') : '未托管'}}" />
          <text wx:if="{{hostUpdatedAt}}" class="text-caption text-secondary">更新 {{hostUpdatedAt}}</text>
        </view>
        <view class="merchant-album-edit__host-actions">
          <button
            wx:if="{{!hosted}}"
            class="btn btn--secondary btn--block"
            loading="{{hosting}}"
            catchtap="onHostAlbum"
          >托管到案例站</button>
          <block wx:else>
            <button
              wx:if="{{hostVisibility === 'public'}}"
              class="btn btn--secondary btn--block"
              loading="{{hosting}}"
              catchtap="onUnpublishHosted"
            >取消公开（仍私密存档）</button>
            <button
              class="btn btn--ghost btn--block"
              loading="{{hosting}}"
              catchtap="onUnhostAlbum"
            >取消托管</button>
          </block>
        </view>
      </ui-card>
        </view>
      </scroll-view>

      <view wx:if="{{showBottomBar}}" class="merchant-album-edit__bottom safe-bottom">
        <view class="merchant-album-edit__bottom-inner">
          <button
            wx:if="{{showSaveButton}}"
            class="btn btn--secondary btn--block merchant-album-edit__bottom-btn"
            loading="{{saving}}"
            catchtap="onSave"
          >保存</button>
          <button
            wx:if="{{showBottomPrimary}}"
            class="btn btn--primary btn--block merchant-album-edit__bottom-btn"
            loading="{{completing}}"
            catchtap="onBottomPrimaryTap"
          >{{bottomPrimaryText}}</button>
        </view>
      </view>
    </view>
  </block>

  <ui-bottom-sheet
    visible="{{visionSheetVisible}}"
    title="{{visionSheetTitle}}"
    hint="{{visionSheetDisclaimer}}"
    showActions="{{true}}"
    scrollable="{{true}}"
    confirmText="知道了"
    cancelText="关闭"
    loading="{{visionSheetLoading}}"
    bind:close="onCloseVisionSheet"
    bind:cancel="onCloseVisionSheet"
    bind:confirm="onCloseVisionSheet"
  >
    <view class="merchant-album-edit__vision" wx:if="{{visionSheetLoading}}">
      <text class="text-caption text-secondary">正在对照本项照片…</text>
    </view>
    <view class="merchant-album-edit__vision" wx:elif="{{visionSheetError}}">
      <text class="text-body">{{visionSheetError}}</text>
    </view>
    <view class="merchant-album-edit__vision" wx:else>
      <text wx:if="{{visionSheetCardTitle}}" class="text-caption text-secondary merchant-album-edit__vision-card">{{visionSheetCardTitle}}</text>
      <text class="text-body merchant-album-edit__vision-summary" selectable>{{visionSheetSummary}}</text>
      <block wx:if="{{visionSheetPlan}}">
        <text class="text-caption merchant-album-edit__vision-label">对门店方案</text>
        <text class="text-body" selectable>{{visionSheetPlan}}</text>
      </block>
      <block wx:if="{{visionSheetGaps.length}}">
        <text class="text-caption merchant-album-edit__vision-label">建议补证</text>
        <text
          wx:for="{{visionSheetGaps}}"
          wx:key="*this"
          class="text-caption text-secondary merchant-album-edit__vision-gap"
        >· {{item}}</text>
      </block>
    </view>
  </ui-bottom-sheet>

  <view
    wx:if="{{partCodePickerVisible}}"
    class="merchant-album-edit__code-picker"
    catchtouchmove="noop"
  >
    <view class="merchant-album-edit__code-picker-mask" bindtap="onClosePartCodePicker" />
    <view class="merchant-album-edit__code-picker-panel safe-bottom">
      <text class="merchant-album-edit__code-picker-title text-h3">选择配件编码</text>
      <text class="merchant-album-edit__code-picker-hint text-caption text-secondary">已按顺序识别 {{partCodePickerImageCount}} 张凭证图，请选择正确编码</text>
      <scroll-view scroll-y class="merchant-album-edit__code-picker-list" show-scrollbar="{{false}}">
        <view
          wx:for="{{partCodeCandidates}}"
          wx:key="pickerKey"
          class="merchant-album-edit__code-picker-item"
          data-index="{{index}}"
          bindtap="onPickPartCodeCandidate"
        >
          <text class="text-body">{{item.displayLabel}}</text>
          <text wx:if="{{item.snippet && item.snippet !== item.partCode}}" class="text-caption text-secondary merchant-album-edit__code-picker-snippet">{{item.snippet}}</text>
        </view>
      </scroll-view>
      <ui-button type="secondary" size="sm" block="{{true}}" bind:btntap="onClosePartCodePicker">取消</ui-button>
    </view>
  </view>
</view>
