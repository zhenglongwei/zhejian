@import '../../../../styles/merchant-form.wxss';

.merchant-order-complete--with-bar {
  padding-bottom: var(--page-fixed-clearance);
}

.merchant-order-complete__body {
  display: flex;
  flex-direction: column;
  gap: var(--space-md);
  padding-top: var(--space-md);
}

.merchant-order-complete__notice {
  margin-top: var(--space-sm);
}

.merchant-order-complete__draft-notice {
  margin-top: var(--space-sm);
}
