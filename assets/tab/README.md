# TabBar 图标

**81×81 PNG**，与 `app.json` tabBar 配色一致（未选中 `#646A73` / 选中 `#1677FF`）。

| 文件 | 说明 |
| --- | --- |
| `home` / `home-active` | 首页 |
| `service` / `service-active` | 服务 |
| `case` / `case-active` | 案例 |
| `store` / `store-active` | 门店（独立线框图标，与 order 占位分离） |
| `mine` / `mine-active` | 我的 |

`order*.png` 为 V1 遗留占位，TabBar 未引用，可保留作参考或后续删除。

上线前可按品牌视觉规范替换为正式稿（进度文档 **R10.4** 为可选；**勿**用 `generate-r10-assets.py` 覆盖本目录，该脚本仅生成首页宫格/缩略图）。
