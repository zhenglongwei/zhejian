---
description: 需求沟通优先——先弄清想法、用可读语言交流、经同意后再改代码。全项目通用，收到任何新需求时必读。
alwaysApply: true
enabled: true
---

# 沟通与协作规则

## 核心原则

**先对齐需求，再动手改代码。** 用户提出新需求时，默认进入「沟通确认」阶段，而非直接进入实现。

## 收到新需求时的标准流程

1. **复述理解**：用一两句话说明「我理解你想做什么、解决什么问题」。
2. **澄清疑点**：目标不清、范围不明、有多种做法、或可能影响现有功能时，先提问；不要凭猜测补全需求。
3. **判断范围**：先分清这是**孤立改动**还是**影响全局**的需求：
   - **孤立**：只动单一页面/接口/文案，不改变跨端口径、数据模型或业务流程。
   - **全局**：触及多端（小程序/H5/后台/backend）、共享组件、快照/脱敏/审核链路、设计体系、合规口径、或 PRD 中的核心流程。
   - 拿不准时，**默认按全局处理**，宁可多查文档也不多改一处漏一处。
4. **全局需求须先对照文档**：若判定为全局影响，动手前须查阅并对照相关全局文档（如 `docs/00_开发计划.md`、对应 PRD、设计体系、模块口径文档），分析：
   - 会影响哪些端、模块、用户路径
   - 与现有规则/快照分层/合规要求是否冲突
   - 有没有已在 PRD 或开发计划里规划过的做法
   - 给出**全局性方案**（统一口径、分步实施顺序、各端如何保持一致），而非只修眼前这一处。
   - **凡需求变更会改到 UI**：必须同时执行规则 `ui-copy-not-prd` 与设计体系 **§2.4.1**——产品口径写进 docs，界面只用版式与短操作提示，禁止把沟通话术贴上屏。
5. **给出方案**：用可读的计划或建议回复，包含：
   - 打算改什么（页面/功能层面，而非堆砌文件名）
   - 为什么这样改
   - 涉及范围与主要取舍
   - 如有多种做法，说明利弊并给出推荐
   - 若是全局需求，须说明影响面与跨端/跨模块的协调方式
   - 若含界面：说明**如何用布局引导**（而非准备写哪些长说明文案）
6. **等待确认**：用户明确同意（如「可以」「按这个做」「开始吧」）后，再编辑代码、运行会改动的命令。

## 沟通语言

- 用**产品/业务语言**为主，技术细节为辅；假设用户不一定是开发者。
- **禁止**堆砌未解释的技术术语、缩写、参数名、内部字段名；确需提及时，紧跟一句白话解释。
- 计划与回复应让**非技术人员也能读懂**；代码引用、文件路径仅在说明「改哪里」时简要使用。
- 避免冗长的实现日志式汇报；优先说清楚「做什么、为什么、需要你决定什么」。

## 可直接动手的情况（无需再等确认）

- 用户已明确授权：「按你说的做」「直接改」「不用问了」
- 本轮对话中用户刚批准了方案，且任务是在执行该方案
- 用户只问问题、要解释、要排查思路，未要求修改
- 用户给出了极其具体、无歧义的修复指令（如贴出报错并要求修这个错）
- 用户明确要求创建 commit / PR（按 git 相关 user rule 执行）

## 禁止事项

- 未弄清需求就开始大范围改代码
- 在方案未经同意时创建/修改多个文件
- 用技术黑话代替对业务影响的说明
- 把猜测当成既定需求默默实现
- 对明显有全局影响的需求，只做局部补丁而不分析跨端/跨模块后果

## 关联规则

- `docs-before-code`（涉及改文档时先改文档）
- `ui-copy-not-prd`（需求变更且改 UI 时）
- `grill-me`（决策未定需严格追问时）
- `feature-build-verify`（对齐后的交付节奏）
