---
description: 数据结构体检——检查业务对象是否自包含、是否存在跨数组按下标/URL 反查配对、工厂是否产全字段、存量兼容是否只读不写回。用于数据/接口/列表结构改完之后、合并前、页面改版后数据出错时。默认只输出报告不直接改代码。
alwaysApply: false
enabled: true
---

# Data Structure Review

与 always 规则 `data-structure-first` 配套：那条管「动手前先定义」，本条管「做完之后体检」。

## 何时执行

- 改完数据对象、接口字段、列表结构后
- 合并前 / 发版前（`release-checklist` 会按需触发本项）
- 页面改版后数据开始错位、串项、丢字段
- 用户提到：结构、字段、串了、错位、丢数据、对不上、多发/少发一项

## 执行步骤

1. **Read** `.codebuddy/rules/data-structure-first/RULE.mdc`（勿凭记忆）。
2. **定范围**：用户指定的对象/页面；未指定则询问，不要全库扫。
3. **找真源**：该对象的文档字段表与常量/schema 真源；没有字段表本身就是一条阻塞问题。
4. **逐项体检**（下表）。
5. **只输出报告**（阻塞 / 非阻塞 + 证据位置）；用户明确要求后再改代码。

## 体检清单

| # | 检查项 | 判「阻塞」的标准 |
| --- | --- | --- |
| 1 | **对象自包含** | 页面上的一整块，字段散在父级与子级两处 |
| 2 | **无跨数组配对** | 出现用数组下标、展示顺序、URL 反查把两份数据拼成一条 |
| 3 | **一对多用数组** | 允许多值却存成单值字段 |
| 4 | **空值语义** | 「空数组」与「字段缺失」语义混用，导致删光被回填 |
| 5 | **工厂产全字段** | 新建对象的工厂/初始化缺业务字段，只给容器字段 |
| 6 | **重排安全性** | 删中间一项或调换顺序，其余项内容会串 |
| 7 | **存量兼容只读** | 读侧兼容旧形状，但新写入仍在产出旧形状 |
| 8 | **单一真源** | 同一对象在小程序端 / backend / admin-web 各有一套字段定义 |
| 9 | **结构不依赖页面** | 结构定义里出现「当前页面只显示 X 个」之类的页面约束 |
| 10 | **跨端一致** | 同一字段在不同端含义或是否多值不一致 |

## 排查手法

- 搜下标配对痕迹：`#${index}`、`urlToIndex`、`draftByKey`、`findIndex` 用于「对齐两条记录」
- 搜反查痕迹：按图片/媒体 URL 去另一份数据里找归属
- 对比「新建对象的工厂产物」与「文档字段表」，列出缺字段
- 造一个最小用例：删中间一项、调序、某项多图，看结果是否仍正确

## 输出模板

```markdown
### 结构体检：<对象名>
范围：<文件/页面>
真源：<文档字段表路径 / 常量文件>

| # | 检查项 | 结论 | 证据 |
| --- | --- | --- | --- |
| 1 | 对象自包含 | 阻塞/非阻塞/通过 | 路径:行号 |

### 阻塞问题
1. …（为什么错、页面结构变化时会怎样）

### 非阻塞建议
1. …

### 结论
可合并 / 需先修第 N 项
```

## 关联规则

- `data-structure-first`（结构与硬规则）
- `docs-before-code`（补字段表时先改文档）
- `release-checklist`（发版前汇总本项）
- `feature-build-verify`（把体检作为「逐项验证」的一环）
