---
description: 非 trivial 功能按「需求对齐 → 拆分可验收项 → 按项写代码 → 逐项验证」交付，禁止一口气堆完再凭感觉说做完。用于新功能、需求变更、跨端/跨步链路（如增项、单据、双端展示）、用户要求按流程开发、拆分功能、写完要验证时。
alwaysApply: false
enabled: true
---

# 功能交付四步（需求 → 拆分 → 写码 → 验证）

与 always 规则配套：`communication-first`（先对齐）、`docs-before-code`（先改真源文档）。
本规则管**动手后的节奏**；需求未对齐时先走 `grill-me` / 沟通确认，**禁止跳过第 1 步直接开写**。

## 何时必须执行

- 新功能、流程改版、跨页面/跨端/跨节点链路
- 用户说：按需求拆分再写、一项项验证、别直接堆代码
- 易出回归的分支（增项、确认两阶段、共享草稿、双端标题等）

**可跳过**（仍须简短自检）：单点 typo、纯文案一词、已批准方案下的机械改字段名。

## 四步（按序，不可颠倒）

### 1. 需求对齐

输出给用户（产品语言，少堆内部字段名）：

- **要解决什么** / **明确不做什么**
- 影响面：单页 / 双端 / 文档真源是否要改
- 歧义先问清；全局需求对照 `docs/` 相关 PRD/口径

未获用户确认（或用户已明确「直接改」）前，**不改代码**。
若会改既有口径：**先更新 docs，再进入第 2 步**。

### 2. 拆分功能

拆成 **3～8 个可独立验收的小项**，每项必须有：

| 字段 | 要求 |
| --- | --- |
| 做什么 | 一句话 |
| 验收点 | 用户或开发如何判定「过了」（可勾选） |
| 主要改动面 | 文档 / 后端 / 商家端 / 车主端 等 |

用 `todo_write`（或等价清单）跟踪；有现成验收文档则挂上（如 `docs/04_维修过程相册/27_DOC-FLOW增项最小验收清单.md`）。

拆完后向用户展示拆分表；**用户未反对或已说按此做**，再写码。

### 3. 按项写代码

- **一次只推进 1～2 个清单项**，完成验证再开下一项（紧急可说明后并行，但验证仍分项）
- 遵守项目设计体系 / token / 组件复用；UI 文案走 `ui-copy-audience`
- 打断当前步的操作（跳转、插节点）必须先落库再跳转
- 双端同一概念用统一展示名，读侧兼容旧数据

### 4. 逐项验证

每项完成后必须留下验证结果（对话中勾选或简表）：

| 项 | 结果 | 证据 |
| --- | --- | --- |
| … | 过 / 未过 / 无法自动测 | 命令输出摘要 / 路径说明 / 需用户点测的步骤 |

- **能自动测的**：跑测试、脚本、关键模块加载；失败则先修再标「过」
- **须真机/点路径的**：写出最短点击路径，标「待你点验」，不要假装已测
- 全部项「过」或「待你点验」已列清之前，**不要声称功能已全部完成**

交付收尾：用 3～5 条概括做了什么 + 哪些需用户点验；关联验收清单路径（如有）。

## 与其它规则

| 场景 | 配合 |
| --- | --- |
| 决策未定 | 先 `grill-me` |
| 改口径 | `docs-before-code` |
| 改 UI 文案 | `ui-copy-audience` |
| 页面交付观感 | `design-system-check` 等检查类（默认可只报告） |

## 反模式（禁止）

- 未对齐需求就大范围改文件
- 拆成技术任务（「改某某.js」）却没有验收点
- 一次提交堆完所有项，最后笼统说「改好了」
- 把「我写了代码」当成「已验证」
- 跳过文档真源更新却改了产品行为

## 输出模板（动手前）

```markdown
### 需求理解
- 要：…
- 不要：…

### 功能拆分
1. … — 验收：…
2. … — 验收：…

### 本轮计划
先做第 x 项；验证后再做第 y 项。
```

## 输出模板（每项完成后）

```markdown
### 已完成：第 x 项
- 改动摘要：…
- 验证：过 / 待点验（路径：…）
### 下一步
第 y 项 / 全部完成待你点验清单
```
