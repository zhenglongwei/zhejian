---
description: 数据结构先于页面——先定义完整的业务对象，再写页面；属于同一业务对象的字段必须住在同一个对象里，禁止跨数组按下标或 URL 反查配对。全项目通用，凡新增/改动数据对象、接口字段、页面列表结构时必读。
alwaysApply: true
enabled: true
---

# 数据结构优先

## 为什么有这条规则

本项目出过一类反复发作的错：页面上一个「项」看起来是一整块（如检测部位 = 部位名 + 多张照片 + 检查结果 + 检查发现），但数据里这四项**不住在同一个对象里**——一部分挂在节点级，一部分挂在草稿级，页面再用数组下标或图片 URL 把它们「对号」拼回去。

后果：页面结构一变（多一行、少一张图、顺序调整），配对就错位，部位和结论互相串台。检测项已修，但同类写法仍可能存在于其他节点、单据和新功能里。

## 核心原则

**结构按业务实体定，不按页面长相定。一个业务对象 = 一个对象。**

页面怎么排版可以随便改；数据结构必须能独立成立，不依赖任何排列顺序。

## 何时必须执行

动手前先 Read 本规则并产出「结构卡」：

- 新增或修改任何数据对象（节点、单据 payload、草稿、接口出入参）
- 页面上一个条目从「一对一」变成「一对多」（如一项允许多张图、多行、多个标签）
- 页面上把两个原本分开的信息合并展示，或把一整块拆开分步填
- 跨端（小程序 / backend / admin-web / H5）传递或共享同一份数据
- 给既有对象加字段、改必填、改多值

**可跳过**（仍须一句自检）：纯文案一词、纯样式、不动数据的展示微调。

## 动手前必须产出的结构卡

用一张表写清，再写页面/接口：

| 字段 | 要求 |
| --- | --- |
| 对象名 | 业务语言命名（如「检测发现项」），不是页面区块名 |
| 字段清单 | 逐字段：含义 / 类型 / **是否数组** / 是否必填 |
| 归属 | 这个字段属于**哪个对象**；两个字段回答同一个「东西」时必须在同一对象 |
| 一对多 | 明确写成数组（如 `images[]`），并说明上限 |
| 空值语义 | 「空数组」和「字段缺失」是否等价（删光了 vs 从未填） |
| 唯一标识 | 靠什么认出「同一条」；**不能**靠数组下标或顺序 |
| 存量兼容 | 旧形状怎么读；新写入是否只写新结构 |

## 硬规则（禁止）

1. **禁止拆分对象。** 属于同一业务实体的字段必须存在同一个对象里。不得一部分放在父级（节点/单据），另一部分放在子级（草稿/配置）。
2. **禁止按位置配对。** 不得用数组下标、展示顺序、图片 URL 反查，把两个数组「对齐」成一条记录。多图就是对象上的一个 `images[]` 数组。
3. **禁止页面反推结构。** 不允许因为「当前页面只显示一张图」就把结构定成单值；结构按业务可能取值定。
4. **禁止下标当主键。** 列表项的增删、排序、筛选不得依赖下标；每项要有自己的标识或完整自包含。
5. **工厂必须产全字段。** 新建对象的工厂/初始化函数，必须产出该对象的**全部业务字段**，不能只给容器字段（如只给 `photos: []` 而缺部位、结果、发现）。
6. **禁止把「散件 + 反查」当结构。** 出现「先存一堆图，再按图去别处找它的结论」的写法，即为违规，必须改造成自包含对象。
7. **存量兼容只读不写回。** 旧形状在 normalize/读侧兼容即可，新写入必须是新结构，不得再产出旧形状。
8. **结构变更先落真源。** 先改文档字段表与常量/schema 真源，再改页面与接口（见 `docs-before-code`）。
9. **禁止多头定义。** 同一对象不得在小程序端、backend、admin-web 各写一套字段；共享结构放单一真源并引用。

## 自检（改完数据相关代码必过）

- [ ] 页面上看到的「一整块」，在数据里是不是**一个对象**？还是有字段散在别处？
- [ ] 有没有任何地方用下标 / 顺序 / URL 把两份数据拼成一条？有即为违规。
- [ ] 删掉中间一项、调换顺序，其余项的内容会不会串？会则结构错。
- [ ] 一对多的地方是不是数组？空数组和缺失的语义是否说清了？
- [ ] 新建对象的工厂是否产出了全部业务字段？
- [ ] 旧数据读侧是否只做兼容、不写回旧形状？
- [ ] 这个结构是否依赖页面当前的排布？页面重排后是否仍成立？

## 关联规则

- `data-structure-review`（改完/合并前的结构体检）
- `docs-before-code`（结构变更先改文档真源）
- `feature-build-verify`（把结构卡作为第 1 步产出）
- `zhejian-development`（各模块真源文档地图）
