---
description: 需求涉及改文档时，先改文档再改代码。全项目通用，凡改动 PRD/口径/设计规范/开发计划/接口说明时必读。
alwaysApply: true
enabled: true
---

# 文档先于代码

## 核心原则

**若新需求会改到既有文档（PRD、口径、设计规范、开发计划、API 说明、规则等），必须先更新文档，再改代码。**

文档是实现依据；先改代码、后补文档，容易造成口径漂移与多端不一致。

## 何时触发

需求涉及以下任一情况时适用：

- 产品行为、业务流程、状态机、权限/合规口径变更
- 设计令牌、组件 API、页面信息架构变更
- 接口契约、字段含义、跨端口径变更
- 开发计划任务拆解或验收标准变更

纯 bug 修复、与文档无关的实现细节、或用户明确说「只改代码不改文档」时，可不改文档。

## 执行顺序

1. **识别**：方案阶段标明要改哪些文档、改什么口径。
2. **改文档**：在用户确认方案后，**先**更新对应 `docs/`（及必要时 `.codebuddy/rules/`）内容，使文档成为新的单一事实来源。
3. **再改代码**：按更新后的文档实现；实现中发现文档仍不准，先回头改文档，再继续写代码。
4. **自查**：交付前确认代码行为与文档一致，无「代码已变、文档仍写旧规则」。

## 禁止事项

- 先大改代码、事后才补文档
- 只改局部代码、不更新已被需求推翻的旧 PRD/口径
- 文档与代码各写一套互相矛盾的说法

## 关联规则

- `communication-first`（先对齐方案）
- `zhejian-development`（文档真源地图）
- `feature-build-verify`（第 1 步即要求先更新文档）
