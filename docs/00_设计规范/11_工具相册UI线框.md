# 工具相册 UI 线框（卷七 · UI-ALB-A-05 / A-06 / A-06b）

> **状态**：产品确认稿 · 2026-06-10（**A-06b** 信息架构修订）  
> **真源**：`00_辙见平台设计体系.md` §10.1.1 / §10.4 · `00_开发计划.md` §7.6  
> **实现前置**：A-07 Token · A-08 组件 API · A-09 PRD

---

## 0. 信息架构总览（A-06b）

**核心分工**：档案元信息在 **列表卡片**；详情页 **只翻图**；说明与隐私在 **信息抽屉**；授权/分享在 **卡片右上角 + 尾页**；合规与联系在 **页底留白栏**。

| 信息/操作 | 列表卡片 `album/list` | 详情翻页 `album/detail` | 信息抽屉 | 尾页（最后一页） | 页底留白栏 |
| --- | --- | --- | --- | --- | --- |
| 标题/服务/门店/车辆/张数 | ✅ 合并展示 | — | — | — | — |
| 状态 Tag（完工/审核/公开） | ✅ | — | — | — | — |
| 分享 | ✅ 右上角 | — | — | ✅ 按钮 | — |
| 授权公示 | ✅ 右上角（与状态合并） | — | — | ✅ 按状态显示 | — |
| 翻页大图 + 节点短文案 | — | ✅ 相框内 | — | — | — |
| 隐私说明 | — | — | ✅ | — | — |
| 门店对图片/节点说明 | — | — | ✅ 全文 | — | — |
| 节点 Tab 跳转 | — | ✅ 底栏 | — | — | — |
| 反馈 | — | — | — | ✅ 按钮 | — |
| 联系门店 | — | — | — | — | ✅ |
| ComplianceNotice | — | — | — | — | ✅ |

**合并规则**

- **标题区 + 摘要表** → 卡片右侧 **档案信息块**（不再在详情/抽屉重复四行表）。
- **公示状态 + 授权按钮** → 卡片右上角 **单一「授权/公示」入口**（文案随 `publicCaseStatus` 变化）。
- **分享** 与授权同排右上角；详情框内顶栏 **不再放分享**。

---

## 1. 我的 · 单页工具台（`pages/mine/index`）

```text
┌──────────────────────────────────────────┐
│ ░ 服务相册 · 门店维修记录          band ░ │
├──────────────────────────────────────────┤
│ ┌────────────────────────────────────┐ │
│ │ [头像]  昵称（选填）                  │ │
│ │         138****5678                  │ │
│ └────────────────────────────────────┘ │
│ ┌────────────────────────────────────┐ │
│ │ [图] 我的服务相册              ● > │ │
│ │      查看门店为你创建的服务相册      │ │
│ │ [图] 我的公开授权                > │ │
│ │ [图] 消息通知                  3 > │ │
│ │ [图] 我的车辆                    > │ │
│ └────────────────────────────────────┘ │
│ ┌────────────────────────────────────┐ │
│ │ [图] 设置                        > │ │
│ └────────────────────────────────────┘ │
│ ┌────────────────────────────────────┐ │
│ │ [图] 使用说明与帮助              > │ │
│ │ [图] 联系客服                    > │ │
│ │ [图] 商家工作台                  > │ │
│ └────────────────────────────────────┘ │
│ 合规说明（浅黄条）                       │
└──────────────────────────────────────────┘
```

未登录：band 同结构；用户区仅「微信一键登录」主按钮。

---

## 2. 相册列表 · 加高档案卡片（`pages/album/list`）

> **A-06b**：列表仍是 **可纵向滚动的多卡片列表**（一屏通常 **2～4 条**，非一屏一本）。  
> **A-06c（澄清）**：在现有 **横向卡片** 上 **加高、充实、美化**——左侧 **较大缩略图预览**（首图/封面），右侧档案信息；**不是**整屏竖版「画册封面」。

### 2.1 列表线框

```text
┌──────────────────────────────────────────┐
│ ░ 服务相册 · 门店维修记录          band ░ │
├──────────────────────────────────────────┤
│  [ 私密相册 ]  [ 公开相册 ]   Tabs        │
├──────────────────────────────────────────┤
│ ┌────────────────────────────────────┐ │  ← 单条卡片（加高约 1.5～2× 现卡）
│ │ ┌──────────┐           分享  授权  │ │  右上：分享 + 授权/公示 §2.2
│ │ │          │  轮胎服务             │ │
│ │ │ 缩略图   │  1号测试店铺            │ │  合并摘要，非四行表
│ │ │ 预览     │  小鹏 X9 · 12 张       │ │
│ │ │ aspect   │  [已完工][审核中]      │ │
│ │ │ Fill     │  更新于 06-08          │ │
│ │ └──────────┘  ▓▓▓▓▓░ 迷你进度       │ │
│ └────────────────────────────────────┘ │
│ ┌────────────────────────────────────┐ │  第二条（结构相同）
│ │ ┌──────────┐           分享  授权  │ │
│ │ │ 缩略图   │  …                      │ │
│ └────────────────────────────────────┘ │
│        … 继续滚动 …                      │
└──────────────────────────────────────────┘

点击卡片主体（除右上按钮）→ 详情翻页
点击分享 / 授权 → 列表内直接操作，不强制进详情
```

### 2.2 尺寸与密度（相对现 `AlbumCard`）

| 项 | 现卡（约） | 目标 |
| --- | --- | --- |
| 布局 | 纯文字，无图 | **左图右文** 横向 |
| 缩略图 | 无 / 极小 | `--size-album-list-thumb`（A-07，建议 **160～200rpx** 宽，与卡同高） |
| 卡片高度 | 偏扁 | **约 200～240rpx** 内容区 + 卡片 padding |
| 一屏条数 | 较多 | **2～4 条**（随机型）；禁止单条占满视口 |
| 相框感 | 无 | 缩略图可选 **轻相框角饰**（小面积，与详情相框 token 同源） |

### 2.3 卡片右上角 · 分享 + 授权/公示（合并）

| 控件 | 显示条件 | 文案/图标 | 行为 |
| --- | --- | --- | --- |
| 分享 | `canOwnerShare` 或已公开可分享 | 线型「分享」图标 | 打开 `ShareSheet`（albumId 上下文） |
| 授权/公示 | 见下表 | **单一按钮**，与状态合并 | 见下表 |

**授权/公示按钮（合并公示状态与操作）**

| `publicCaseStatus` | 按钮文案（示例） | 行为 |
| --- | --- | --- |
| `private` / 可授权 | 「授权公示」 | 打开授权流程（BottomSheet 勾选+提交，或跳授权页） |
| `pending_review` | 「审核中」 | 弱态；可点开说明「公开申请审核中」 |
| `public_approved` | 「已公开」 | 可点开「查看公示页」或弱化仅 Tag |
| `user_rejected` | 「授权公示」 | 同可授权 |
| 未完工 / 无图 | **不显示** | — |

图标+短文案同排；Tag 行只读状态，右上角为可操作入口（语义不重复堆砌）。

### 2.4 升级前后对比

| 维度 | 现 `AlbumCard` | 升级后（仍叫 `AlbumCard` / `ui-album-card`） |
| --- | --- | --- |
| 形态 | 扁文字卡 | **加高横向卡**，列表可滚动多条 |
| 图片 | 无预览 | **左侧缩略图**（首节点首图 / `coverUrl`） |
| 信息 | 分散多行 caption | 服务名突出 + 门店/车辆/张数 + **送修日期 + summaryLine** |
| 操作 | 商家端有分享 | 用户端右上 **分享 + 授权/公示** |
| 进度 | 无 | 可选六段迷你进度条 |

**阶段 E · 摘要行（列表卡 meta 第二行）**

| 字段 | 示例 | 规则 |
| --- | --- | --- |
| `deliverDateText` | 送修 06-08 | 卡片日期行，优先于「更新于」 |
| `summaryLine` | 制动异响 · 刹车片×2 · 参考¥1280 | 单行省略；见 PRD `07_…` §14.4 |

空态（私密）不变：暂无服务相册 · 扫码提示 · H5 公开案例链（`ui-empty`）。

### 2.5 命名说明

- 产品口语「相册封面」= **卡片上的缩略图预览区**，不是全屏封面页。
- 实现上 **升级** `components/album-card`，**不新增**占满全屏的 `AlbumCoverCard` 组件类型。

---

## 3. 相册详情 · 全屏相框翻页（`pages/album/detail`）

> 主阅读 = **全屏相框 + 翻页**；**无**置顶摘要、**无**框内分享、**无**底栏授权按钮。  
> 组件：`ToolImmersiveNav` + `AlbumFrameViewer` + `AlbumBottomToolbar`（仅节点 Tab）+ `AlbumPageFooter` + `AlbumInfoSheet`。

### 3.1 页面态

| 态 | 布局 |
| --- | --- |
| `loading` | 沉浸导航 + 相框 Skeleton |
| `normal` | 翻页（含尾页 §3.6）+ 底栏 Tab + 页脚 |
| `empty` | 无过程图：Empty + 页脚（联系门店+合规） |
| `error` | Empty + 重试；无分享入口 |

### 3.2 主态线框

```text
┌─ status bar ─────────────────────────────┐
├─ ToolImmersiveNav ───────────────────────┤
│  ← 返回              服务相册              │
├──────────────────────────────────────────┤
│░╔══ corner ═════════════════ corner ══╗░│
│░║ ▓┌──────────────────────────────┐▓ ║░│
│░║ ▓│ 3 / 13              信息      │▓ ║░│  仅页码 + 信息（无分享）
│░║ ▓│                              │▓ ║░│
│░║ ▓│         大图 / 尾页           │▓ ║░│  swiper：图片页 + 虚拟尾页
│░║ ▓│                              │▓ ║░│
│░║ ▓├──────────────────────────────┤▓ ║░│
│░║ ▓│ 接车记录 · 06-08 14:30       │▓ ║░│  图片页：短文案条（§3.4）
│░║ ▓└──────────────────────────────┘▓ ║░│  尾页：无文案条，见 §3.6
│░╚══ corner ═════════════════ corner ══╝░│
├─ AlbumBottomToolbar（半透明）────────────┤
│  接车 · 检测 · 施工* · 完工 · 交车        │  仅节点 Tab（暂定保留）
├─ AlbumPageFooter（页底留白栏）───────────┤
│  [联系门店]                              │
│  ComplianceNotice（浅条，详情页唯一位）    │
├─ safe-area ──────────────────────────────┤
└──────────────────────────────────────────┘
```

页码 `total` = **图片张数 + 1（尾页）**。

### 3.3 相框（`AlbumPhotoFrame`）

与 A-06 一致：四边等宽、角饰 L 形、立体阴影 `--shadow-album-frame`；艺术字仅框内短标签（≤4 字）。

### 3.4 图片页 · 框内区域

#### 翻页

- 横向 swiper；无露边、无大箭头；禁止 `previewImage`。
- 页序列：`[图1…图N]` + **尾页**（§3.6）。

#### 框内顶栏

| 元素 | 行为 |
| --- | --- |
| 页码 | `{index} / {total}` |
| 信息 | 打开 `AlbumInfoSheet`（§3.7） |

**无分享按钮**（分享在列表封面右上角 + 尾页）。

#### 框内底文案条（仅图片页）

| 字段 | 规则 |
| --- | --- |
| 节点名 | 当前页节点 `title` |
| 时间 | 节点更新时间 |
| 节点说明摘要 | **最多 1 行**省略；全文见信息抽屉 |

**无**「对此节点有疑问」链接（反馈移至尾页 §3.6）。

### 3.5 底栏 · 节点 Tab（`AlbumBottomToolbar`）

| 内容 | 说明 |
| --- | --- |
| 节点 Tab 横滑 | 当前节点高亮；点击跳到该节点第一张 |
| **已取消** | ~~授权公示按钮~~（A-06b） |
| **已取消** | ~~联系门店~~（移至 §3.8 页脚） |

### 3.6 尾页 · 空白结束页（`AlbumEndPage`）

翻到最后一张图后再滑一页，进入 **虚拟尾页**（无照片，暖底 + 轻相框内留白）。

```text
┌────────────────────────────────────┐
│                                    │
│         （无大图，档案纸底）        │
│                                    │
│    ┌──────────┐  按状态显示        │
│    │ 授权公示 │  或「审核中」说明   │
│    └──────────┘                    │
│    ┌──────────┐                    │
│    │   分享   │                    │
│    └──────────┘                    │
│    ┌──────────┐                    │
│    │   反馈   │  → 相册反馈页      │
│    └──────────┘                    │
│                                    │
└────────────────────────────────────┘
```

| 按钮 | 显示 | 行为 |
| --- | --- | --- |
| 授权公示 | `shouldShowAuth` | 同列表封面授权流程 |
| 分享 | 有可分享能力 | `ShareSheet` |
| 反馈 | 始终 | `album/feedback` |

尾页 **不显示** 框内底文案条；页码显示为最后一页（如 `13 / 13`）。

### 3.7 信息抽屉（`AlbumInfoSheet`）· 阶段 E

> **2026-06-13 修订（UI-ALB-E-01）**：详情 **恢复** 半屏抽屉；**仍禁止**在翻页区堆 KeyInfoTable。  
> 主阅读仍为全屏相框；结构化字段 **只在抽屉** 与 **列表 summaryLine** 展示。

半屏 BottomSheet（约 **55–65%** 屏高）；标题「说明与档案」。

```text
┌──────────────────────────────────────────┐
│  说明与档案                        ✕     │
├──────────────────────────────────────────┤
│  [PrivacyBanner scene=album]              │
│  ─────────────────────────────────────   │
│  档案摘要（KeyInfoTable）                 │
│  送修日期 │ 2026-06-08                    │
│  里程     │ 32,000 km                     │
│  故障/损伤│ 制动异响                      │
│  …                                        │
│  ─────────────────────────────────────   │
│  配件清单（有 partsJson 时）              │
│  名称      类型      数量                  │
│  刹车片    品牌件    ×2                    │
│  ─────────────────────────────────────   │
│  门店说明                                │
│  ┌────┬─────────────────────────────┐   │
│  │缩略│ 接车记录                      │   │
│  │图 │ 门店对该节点说明全文…          │   │
│  └────┴─────────────────────────────┘   │
│  … 可纵向滚动 …                          │
└──────────────────────────────────────────┘
```

| 区块 | 内容 |
| --- | --- |
| PrivacyBanner | 相册隐私短提示 |
| KeyInfoTable | `summaryRows` 档案字段，见 PRD §14.4 |
| 配件清单 | `partsJson` 表格式；无数据则不展示该区块 |
| 门店说明列表 | 有 `note` 的节点：缩略图 + 节点名 + **说明全文** |
| 门店总说明 | 若有 `storeNote`，置于「门店说明」区置顶 |

**不在抽屉内**：Tag、授权表单、分享、反馈、ComplianceNotice（合规归工具页 shell / 协议）。

**入口（阶段 E 实现）**：沉浸 chrome 显隐时，顶栏右侧 **「档案」** 文字按钮（或等价入口）；尾页不提供抽屉入口。

数据：Phase 1 节点级 `note` + `partsJson` + `vehicleJson`；聚合由 **B-ALB-08** 提供。

### 3.8 页底留白栏（`AlbumPageFooter`）· 已下线

> **§7.6.0（2026-06-11）**：详情 **不再使用** 页脚组件；联系门店 → **尾页**；ComplianceNotice → 工具页 shell / 设置协议。组件保留供其他场景复用。

### 3.9 交互清单

| 操作 | 结果 |
| --- | --- |
| 左右滑 | 图片页 / 尾页 |
| 轻触画布 | 显隐 chrome（顶栏进度 + 底栏说明） |
| 顶栏 · 档案 | 打开信息抽屉（阶段 E） |
| 底栏进度段 | 跳到对应阶段首张 |
| 尾页 · 授权 | 授权流程 |
| 尾页 · 分享 | ShareSheet |
| 尾页 · 反馈 | 反馈页 |
| 尾页 · 联系门店 | 拨号 |
| 列表封面 · 分享/授权 | 不进入详情即可操作 |

### 3.10 禁止项

- 详情置顶摘要卡、**翻页区内** KeyInfoTable / 配件表  
- 框内顶栏分享、主屏 PrivacyNotice、底栏授权按钮  
- 促销模块、时间线网格主视图  
- 详情页 `AlbumPageFooter` / 主屏 ComplianceNotice（§7.6.0）

---

## 4. 验收对照

> **详情真机逐项清单**：`docs/12_测试验收部署与安全合规/UI-ALB-C-08_相册详情真机验收清单.md`（UI-ALB-C-08）

| 屏 | 必见元素 |
| --- | --- |
| 我的 | band、图标 Cell |
| 列表 | 加高横向卡、左缩略图、右上分享+授权、一屏多条 |
| 详情 | 全屏相框、翻页、尾页三按钮、节点 Tab、页脚联系+合规 |
| 抽屉 | 仅 PrivacyBanner + 门店说明列表 |
| 全站 | 无促销 Banner；主色 ≤15% |

---

## 5. 组件与页面映射

| 区域 | 组件 | 页面 |
| --- | --- | --- |
| 列表卡片 | `album-card`（加高 + 缩略图 + 右上操作） | `album/list` |
| 沉浸导航 | `tool-immersive-nav` | `album/detail` |
| 相框 | `album-photo-frame` | viewer 内 |
| 翻页+尾页 | `album-frame-viewer` + `album-end-page` | `album/detail` |
| 节点 Tab | `album-bottom-toolbar` | `album/detail` |
| 页脚 | `album-page-footer` | `album/detail` |
| 说明抽屉 | `album-info-sheet` | `album/detail` |
| 页数据 | `buildAlbumFlipPages` + 虚拟尾页 | `utils/album-flip-pages.js` |

---

## 6. 版本

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| A-05 | 2026-06-10 | 我的 / 列表 / 初版详情 |
| A-06 | 2026-06-10 | 全屏相框翻页 + 初版抽屉/底栏 |
| A-06b | 2026-06-10 | 列表档案信息+右上操作；抽屉极简；尾页；页脚合规 |
| A-06c | 2026-06-10 | 列表澄清：加高横向卡+缩略图，非一屏一本 |
| C-08 | 2026-06-11 | 详情真机验收清单 → `12_…/UI-ALB-C-08_相册详情真机验收清单.md` |
