# 案例档案与托管状态机

> **生效**：2026-09-03  
> **修订**：2026-09-11 · 通用档案口径；**2026-09-12** · 公开档案页阅读（§7.9）；案例站页面地图（§7.10）  
> **状态**：产品口径定稿 · 实现主真源  
> **配套**：[`14_四件套产品架构与定位.md`](14_四件套产品架构与定位.md) · [`19_商家发布案例上网口径.md`](../04_维修过程相册/19_商家发布案例上网口径.md) · [`16_主流大模型GEO策略画像与提示词要点.md`](../09_SEO_GEO_AI内容基础设施/16_主流大模型GEO策略画像与提示词要点.md)

---

## 1. 对象与分层

**案例档案（Case Archive）** = 一次维修在案例站的托管单元，分三层：

| 层 | 内容 | 可改？ |
| --- | --- | --- |
| **事实层** `archiveSnapshot` | 相册基础信息、**`storeSnapshot`（托管时门店快照）**、过程图/节点、DOC-FLOW 全链单据 payload、车主确认件快照 | **托管后只读**；不含旧 `merchantCaseDraft` 优化文稿；门店主档仍在账号侧维护 |
| **可公开面** `publicFace` | 门店提交公开的图/文（自处理或经平台脱敏工具）；须过隐私规则 | 公开前可调整可公开面；不改动事实层字节 |
| **GEO 层** `geoLayer` | 摘要、亮点、本单 FAQ | 门店可改；**确认 GEO 后**才公域上线（公开托管路径） |

公开页展示：GEO 层 + 可公开面（脱敏图文/单据）+ FAQ。

### 1.1 门店归属（账号主档 + 案例快照）

| 层 | 放什么 | 说明 |
| --- | --- | --- |
| **账号 / 门店主档** | 店名、地址、资质、联系方式等 | 入驻后持续可改；案例库登录身份 |
| **每条档案 `storeSnapshot`** | 托管瞬间的 `storeId` + 店名/城市/地址摘要 | 冻结只读；单条案例可独立展示，不随主档改名漂移 |

**现阶段生产**：仅门店账号生产与托管（见 §3）。档案须自描述门店（`storeSnapshot` 或声称门店）。**个人交档、第三方以个人名义交档暂不实现**，数据结构按 §7 预留，不另做一套网站或版式。

---

## 2. 状态维度

| 维度 | 取值 | 含义 |
| --- | --- | --- |
| `hosted` | false / true | 是否托管到案例站 |
| `visibility` | private / public | 是否公域可见 |
| `publicPublishStage` | — | 公开托管进度：`awaiting_privacy` → `awaiting_geo` → `awaiting_geo_confirm` → `published` |
| `privacyAuditPassedAt` | null / ISO | 隐私规则校验通过时间 |
| `useDesensitizeTool` | bool | 是否使用平台脱敏工具（可选；责任仍在门店） |
| `geoLayer.confirmedAt` | null / ISO | 门店确认 GEO 文案时间 |

约束：

- `visibility=public` ⇒ `hosted=true` 且 **隐私规则已通过** 且 **GEO 已确认**（公开托管路径）。  
- **取消**真实性承诺勾选（全平台废止）。  
- 私密托管：**不**生成 GEO。  
- 取消公开：`visibility=private`，档案仍托管。  
- 取消托管：`hosted=false`；公域下线。

---

## 3. 主流程

```text
服务相册完工（completed）
    → [托管到案例站]
         ├─ 私密托管：hosted=true, visibility=private（不生成店页文案）
         └─ 公开托管（意图）：
              hosted=true, visibility=private, stage=awaiting_privacy
              → 【核对内容】按报告顺序通读本单图文（检测/方案/工单/完工）；可选「自动脱敏 / 手工打码」或门店自认已处理
              → 隐私规则校验通过
              → 【店页文案】LLM 按三家并集提示词从可公开面提取摘要/亮点/FAQ；门店可改
              → 门店确认文案 → visibility=public，写入店页
    → [改回仅私密]（未上店页：清空公开进度；已上店页：取消公开仍托管）
    → [取消托管] hosted=false；公域下线
```

**隐私责任**：发布者对公开内容负责；平台提供脱敏工具与隐私规则校验，不代门店担责。  
**店页文案**：见 [`16_主流大模型GEO策略画像与提示词要点.md`](../09_SEO_GEO_AI内容基础设施/16_主流大模型GEO策略画像与提示词要点.md)（三家并集提示词 v0）。

---

## 4. 确认件与修订

| 对象 | 规则 |
| --- | --- |
| **确认件** | 车主确认瞬间冻结；写入 `archiveSnapshot`；不可静默覆盖 |
| **事实层** | 托管时整包冻结；不得静默改 |
| **GEO / 店页文案层** | 可修订；公域以最新确认版为准 |

---

## 5. 与旧「生成案例稿」关系

- **废止主路径**：本地生成优化 `merchantCaseDraft` 再推送案例站。  
- **新主路径**：原始服务档案托管 →（公开时）核对公开内容 → 店页文案 → 确认上线。  
- 存量 `merchantCaseDraft` / `case-draft` 页只读兼容；新单走托管向导。

---

## 6. 入口与托管管理页

- **DOC-FLOW 完工页 / 编辑页只读**：已完工 → 「托管到案例站」/「管理托管」。  
- **托管管理页（三步，商家口吻）**：  
  1. **存档** — 私密 / 公开；公开进行中可「改回仅私密」  
  2. **核对内容（通读预览）** — **按报告顺序**完整展示本单图文；**点图在本页打码**，打完立即显示打码图；也可「一键 AI 脱敏」；「我已核对，继续」  
  3. **店页文案** — 摘要/要点/常见问法（LLM 提取，可改）；确认后上店页
- 常见问法：按服务类目预置消费者常问；有本单料则答「惯例→本单→差别」；薄案例**只出题、答留空**给商家补；**公开页仅展示有答条目**。
- 摘要 / 要点 / 常见问法：**专业且亲切友好、像人话**（面向看店页的车主）；禁止公文腔、机器腔。
- 要点或常见问法整块为空时：**仍展示分区**与原因，并提供添加入口。
- 界面不出现「GEO / 校验」等实现黑话；用「店页说明」「将公开的内容」等操作短句。

---

## 7. 通用档案口径（2026-09-11）

> 共识来源：grill-me。目标：尽量全、来源可追溯的汽修维修案例库；**首要给 AI 引用**，其次才是人翻看。门店与消费者都从中获益。平台**不盖章、不审真伪**。

### 7.1 一种档案、按材料显隐

全站公开页只有一种「一次维修的档案」，不按小程序 / 微信转案例 / 第三方换版式。

固定分区（有则展示，没有整块拿掉，不编满）：这次修了什么、过程证据、单据、提交者与门店归属、常见问法。微信整理出的段落只进概况或问法，不是另一种「文章页」。

### 7.2 工具归收料，身份归署名

档案算谁的，只看**推送到案例站时登录的案例库账号**（门店或个人），不看生产工具自己的用户名。

底线（所有工具，含第三方，类 GitHub `git push`）：

- 用工具干活（写、拍、整理）**不必**先有案例库账号；工具里可以随便有自己的用户名。  
- **托管 / 推送**必须登录案例库账号。这时才区分门店还是个人，才决定进店页还是全球库声称路径。  
- 工具本地用户名不等于案例站提交者。

| 交档身份（推送时的案例库账号） | 归属 | 进店页？ | 给机器的归属 |
| --- | --- | --- | --- |
| **门店身份**（账号已绑定该店） | 该店自己的档案 | 过隐私 + 确认店页说明后 **进** | 「该店发布 / 该店案例」 |
| **个人 / 未绑店** | 只能声称某店 | **不进** | 「提交者声称、门店未确认」；标题只写车型 + 项目，**不得**写成「这家店的案例」 |

不要给每个工具单独写归属规则。

### 7.2.1 自有工具多端互通；第三方不通

**辙见账号**：案例站与自有工具共用的一套身份。门店或个人写在这个账号上（个人可再绑定门店，像加入组织），不写在「从哪个端注册」。

| | 规则 |
| --- | --- |
| **自有端** | 案例站、小程序门店端、微信转案例（及车主端若登录）**一个账号打通**。在任一端注册（网站或小程序），用同一手机号或同一微信合成一个号，即可使用全部自有工具。已在自有端登录时，托管可不再重复弹案例库登录（第一次仍须登录或绑定）。 |
| **第三方** | **不做**多端互通：不能用第三方的登录态打开小程序或案例站后台。只在推送那一次登录案例库账号。第三方可有自己的用户名。 |

未绑店的微信转案例：未登录案例库时按工具自用；一旦推送且账号未绑店 → 声称路径；账号已绑店 → 门店路径。

### 7.3 店页与全球库

| 表面 | 收什么 |
| --- | --- |
| **店页（橱窗）** | 仅该店**自己托管**或**已经认领**的档案 |
| **全球案例库** | 过了 §7.4 底线的公开档案，含未认领（须标声称） |

库首先给 AI 引用：全球库里**能打开即可被抓取、被引用**，不因「未认领」再设第二道引用门槛。未认领只限制**怎么写店名**，不限制能不能被引用。

### 7.4 进全球库底线

须同时具备：

1. **谁提交的**  
2. **声称哪家店**（店名 + 城市；对得上入驻店更好，对不上也可先未认领）  
3. **修的是什么**（车型、项目、故障至少能看懂一件）  
4. **至少一类过程证据**：现场图、单据，或可核对的经过说明（群聊整理算这一种）

公开前由**提交者**核对将上网的内容并过隐私过滤。不要求四阶段拍全、单据齐全、店家已认领、问法写满。

门店身份走公开托管时：仍须确认店页说明（§3 / §6），然后进该店店页。

### 7.5 认领

认领 = 门店**同意挂到本店店页**，并开始对「这是本店的案例」这份署名负责。

- 原提交者交来的图、单据、经过说明 **只读**，并保留「谁提交的」。  
- 门店可以**另补**店页说明、另补自己的材料，不能改掉别人已交的事实。  
- 不认领 = 不上店页；全球库仍可打开、可被引用，店名只作为声称。

**门店不能自己**给未认领档案摘掉店名或下架。要动这一页，只走投诉（隐私、违法、根本不是维修档案等），由平台处理。门店若要回应，自己再交一份档案（两份并存、各署各名）。

### 7.6 两份就是两份

两个人（或两次提交）交上来的就是两份独立档案：独立网址、独立署名、独立可引用。平台**不合并**、不认定「同一单」、不提示「可能为同一维修」。

人看的档案页**下方**可挂「相似案例 / 相关案例」链接，便于对照。不为 AI 单写「相似」关系或专用字段。

### 7.7 星标（抄 GitHub 到档案层）

- 只做**星标**，不做点赞、有用、没用；收藏并进这一个动作。  
- 星标打在**这一份档案**上：点了进入「我的星标」，公开数字 +1；再点取消。  
- **不加总**到门店或用户，不做「本店 XXX 星」或评分。  
- 数字公开。名单：门店出**头像（店标）+ 店名**（链到店页）；个人出**头像 + 昵称**（或未设头像时用默认图）。头像由门店/个人在主页**自行设置**，不默认同步微信实名头像或证件照。  
- 个人隐私（手机号、真实姓名、证件等）：星标名单和**别人打开的个人主页都看不到**，只有本人能看。
- **界面抄 GitHub 仓库页**：星标是标题右侧的按钮（图标 + 星标/已星标 + 数字）；名单用重叠头像，悬停看店名/昵称。正文**不放登录表单**。未登录点星标（或顶栏「登录」）弹出登录层，成功后立刻关掉并完成刚才的动作。

语义：关注 / 认可**这份档案**，不是评价这家店。给机器的是档案上的星标次数，禁止叫评分、好评、推荐指数。

### 7.8 店页企业资质

门店自己上传、自己勾选公开；**平台不验真、不背书**。

- 优先依法应公示的企业信息：店名、统一社会信用代码，并可链到国家企业信用信息公示系统。  
- 需要图时放带平台水印的执照 / 备案图。  
- **不上**个人身份证、技师证件号、人脸。  
- 过期由门店更新；过期仍挂着不当成平台保证有效。  
- 给机器的字段写成「门店自行公示」，不要写成「平台已认证」。

执照上随企业依法公示出现的法定代表人姓名，可随执照出现，不单独做成个人档案去扩散。

### 7.9 公开档案页怎么读（H5）

公开页给人看的是**已完成档案**，不是车主确认中的进度页，也不是门店填表页。

| 给谁 | 看见什么 |
| --- | --- |
| 门店生产 | 拍照步 + 单据整链 |
| 车主进度 | 单据链（检测报告 → 方案 → 工单 → 完工确认）；照片作附件 |
| **公网档案** | 同一套单据名 + **单独一章施工过程图**；无金额、无确认 |

接车图进检测报告，交车图进完工；不要再单独铺「接车与检测 / 完工照」。电脑端两列排版，星标在标题旁，右侧只留地址（可开地图）和电话。细则见设计体系 §10.8.2。

### 7.10 案例站页面地图（H5 · 2026-09-12）

> 辙见案例站是**维修案例托管库**（类 GitHub），不是预约促销站、不是门店黄页。  
> 视觉仍遵守 north star：像可翻阅的服务相册。平台不盖章、不审真伪，店页不做资料打分。

| 页面 | URL | 给谁 | 干什么 |
| --- | --- | --- | --- |
| **探索首页** | `/` | 所有人 | 一句话定位 + 搜索入口 + **最新公开档案**。不设门店广场、不主打预约 |
| **公开案例** | `/case/` | 所有人 | 全球库：已公开托管的档案列表 |
| **档案页** | `/case/{slug}.html` | 所有人 | 一份维修档案（§7.9） |
| **展示页** | `/store/{id}.html` | 所有人 | 该账号对外橱窗（现阶段即门店主页）：简介 + **本店公开档案** + 自行公示的资质。地址可开地图，可留电话 |
| **我的** | `/library/` | 登录用户 | 私人主页：改显示名、看/管托管档案与星标；链到自己的展示页。`noindex` |
| **搜索** | `/search/` | 所有人 | 按关键词找档案 / 门店 / 服务；门店结果进展示页 |

**已删除独立门店列表页**（原 `/store/`、`h5/store/index.html`、`store-list.js`）。旧书签 **301 到首页**。顶栏不放「门店」。找店走搜索或档案上的店名。展示页 `/store/{id}.html` 保留。

展示页抬头：**Logo 小标 + 店名同行**，简介跟在店名下；电脑端不要通栏大海报。托管列表「公开/私密」与公网是否可见一致；点托管卡片进档案页。搜索页以搜索框为主。旧误链 `/cases/` **301** 到 `/case/`。

**公开读侧**：门店完成公开托管（已托管 + 隐私过线 + 已确认店页说明）后，档案即出现在展示页、公开案例列表、首页，并能打开详情。不再另等旧「文章已发到网站」一步。

**本轮不做**：个人交档与未绑店个人展示页（仍 §7.2 / HOST-P4-PERSON）；不改档案 URL / Schema 字段名。
