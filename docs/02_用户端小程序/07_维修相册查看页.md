```


---

```markdown
# 《服务相册查看页 PRD》

---

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档名称 | 服务相册查看页 PRD |
| 所属模块 | 用户端小程序 |
| 当前版本 | V2.0 Phase 1 |
| 对应主文档 | `01_用户端小程序PRD V2.0.md` |
| 口径 | `docs/04_维修过程相册/00_Phase1_服务相册产品口径.md` |
| 适用端 | 微信小程序用户端 |
| 页面范围 | 服务相册列表（加高卡片）、详情清单阅读（下线整本全屏翻页）、页底操作区（分享/公开发布/评价等） |
| 文档目标 | 用户 **查看服务相册** 与 **授权公开**（Phase 1 不要求配件确认） |

---

## 2. 模块定位

服务相册 / 服务相册是 V2.0 平台核心能力（**服务相册 + 可选公开授权**）。

它的目标是：

1. 让用户看到线下维修 **留档**（六阶段、配件录入、参考价）；
2. 降低对维修不透明的担忧；
3. 沉淀可回溯的服务相册（**争议证据**）；
4. 在用户 **授权公开** 后，生成脱敏公开案例；
5. 为 GEO 内容与咨询转化提供素材。

**Phase 1**：查看留档为主；**唯一车主主动操作** = 授权公开（勾选 + 留痕）。  
**Phase 2**：配件/方案待确认。  
默认私密，未经授权不得公开。不绑定平台订单。

---

## 3. 页面范围

本文档覆盖：

```text
服务相册入口
服务相册首页
按维修节点查看图片
图片大图预览
图片说明
上传时间展示
门店备注
用户反馈入口
服务相册入口
申请公开/授权公开入口
脱敏提示

```

不覆盖：

```
商家上传服务相册
平台脱敏审核后台
公开案例详情页（见 05_H5/02）
```

---

## **4. 页面入口**

用户可从以下入口进入服务相册：

1. **我的 → 我的服务相册**；
2. 商家分享的档案链接（需登录且为关联用户）；
3. ~~配件待确认消息~~（Phase 2）；
4. 服务通知跳转（档案更新、可授权公开）。

---

## **5. 权限规则**

### **5.1 可查看用户**

仅以下用户可查看私密服务相册：

1. 订单本人；
2. 平台客服/审核人员，后台权限；
3. 对应门店商家，后台权限；
4. 法务/售后处理人员，后台权限。

### **5.2 不可查看用户**

1. 非订单本人；
2. 通过转发链接进入的陌生用户；
3. 未登录用户；
4. 登录但无订单权限用户。

### **5.3 无权限提示**

```
你无权查看该服务相册。

```

### **5.4 公开后访问**

如果用户授权公开并审核通过，外部用户只能查看：

```
脱敏后的公开案例页

```

不能查看原始私密相册。

---

## **6. 相册页面结构**（UI-ALB · 2026-06-11）

**列表页** `pages/album/list`：

```text
页顶 band
Tab：私密相册 | 公开相册
加高横向卡片列表（左缩略图 + 档案信息 + 右上分享/授权）
```

**详情页** `pages/album/detail`：

```text
沉浸导航 ToolImmersiveNav
清单阅读 album-work-checklist（合成卡 + 阶段分组图；点图单张全屏）
页底操作区 album-end-page（原尾页：分享 / 发布 / 评价 / 联系门店等）
（历史：全屏相框翻页 AlbumFrameViewer 已下线）
```

**信息分工（2026-08-13）**：档案元信息在 **列表卡片**；详情以 **清单阅读** 为主；授权/分享/评价在 **页底操作区**；点图仅单张全屏，不再整本翻页。

---

## **7. 服务与车辆摘要**（V2.0）

展示：


| 字段   | 说明   |
| ---- | ---- |
| 服务名称 | 必填   |
| 门店名称 | 必填   |
| 相册状态 | 必填（进行中/配件待确认/已完工等） |
| 车辆品牌 | 必填   |
| 车型   | 必填   |
| 车牌   | 脱敏展示 |
| 维修时间 | 可选   |
| 图片总数 | 可选   |


车牌展示：

```
浙A****8

```

---

## **8. 维修节点**

### **8.1 节点分类**

MVP 建议节点：

```
到店接车
检测诊断
拆检过程
施工过程
配件更换
完工检查
交车确认

```

不同服务可配置不同节点。

### **8.2 标准服务节点**

如小保养、电瓶、轮胎：

```
到店接车
施工前
施工中
完工检查
交车确认

```

### **8.3 钣喷维修节点**

```
损伤确认
拆装过程
钣金修复
底漆/腻子
喷漆过程
抛光检查
完工对比

```

### **8.4 事故车维修节点**

```
到店检测
损伤确认
拆检过程
维修方案确认
结构/钣金修复
配件更换
喷漆修复
安全检查
完工检查

```

### **8.5 节点字段**


| 字段     | 说明          |
| ------ | ----------- |
| 节点名称   | 必填          |
| 节点状态   | 未开始/进行中/已完成 |
| 上传图片数  | 必填          |
| 节点说明   | 可选          |
| 更新时间   | 可选          |
| 是否关键节点 | 可选          |


---

## **9. 图片列表**

### **9.1 图片字段**


| 字段        | 说明    |
| --------- | ----- |
| 图片 ID     | 必填    |
| 缩略图 URL   | 必填    |
| 原图/大图 URL | 受控访问  |
| 节点 ID     | 必填    |
| 上传时间      | 必填    |
| 上传方       | 门店/平台 |
| 图片说明      | 可选    |
| 审核状态      | 内部字段  |
| 是否可公开     | 内部字段  |
| 脱敏状态      | 内部字段  |


### **9.2 展示规则**（UI-ALB · 2026-08-13 修订）

1. **详情主视图**：清单阅读；点图进入**单张全屏**，再点关闭；**下线**整本 `AlbumFrameViewer` 相框翻页；
2. **页底操作区**：清单读完后的收束区 — **服务评价**（完工未评主按钮「评价」）/ 联系门店 / 发布相关状态提示与下架等；**不再**在页底放「分享」「检查与档案」「配件验真」文字链（分享仍可从列表卡等入口进入；检查与档案 / 配件验真本期不在详情页底暴露）；
3. **清单阅读 IA（2026-08-14）**：以**检查项**为阅读单元；类目仅作分段小标题；检测父项与施工衍生项（如机油机滤）合成一张主题卡；结果只在项上展示一次；图下仅本图说明；无证据项不展示；不按结果聚卡；
4. 无清单老相册按节点过程图做简易阅读卡；
5. 图片加载失败展示占位；审核不通过图片不展示；原始图片不得暴露公开 URL。

---

## **10. 图片预览**

### **10.1 大图预览能力**

用户点击图片后进入大图预览。

支持：

1. 左右滑动；
2. 查看图片说明；
3. 查看上传时间；
4. 保存图片，是否允许需配置；
5. 问题反馈；
6. 返回相册。

### **10.2 保存图片与下载档案规则**

> **定稿 2026-07-25**：车主私密留档支持 **一键下载全部原图压缩包**，符合「服务相册 = 留档工具」定位；**禁止**经分享/H5/公域传播原图。

| 能力 | 规则 |
| --- | --- |
| **入口** | 「我的服务相册」列表页底栏 **下载档案**（低频）；进入单选模式后点选一本相册并确认 |
| **权限** | 仅**已关联车主**；商家端本能力不做 |
| **内容** | 该相册 `AlbumImage` 全部**原图**（非脱敏图） |
| **命名** | 按节点名：单张 `接车记录.jpg`；同节点多张 `接车记录-1.jpg`、`接车记录-2.jpg`…；配件图亦按**节点名** |
| **交付** | 服务端打包 ZIP → 小程序下载后引导转发至「文件传输助手」等保存 |
| **合规文案** | 须提示「仅供本人留档，请勿公开传播原图」 |

单张预览页的「保存到系统相册」仍可选；与压缩包归档互补。商家上传侧可继续加轻水印（既有能力），不影响车主下载原图留档。

### **10.3 图片水印**

私密相册图片可加轻水印，防止被误用。

水印内容可包括：

1. 平台名称；
2. 上传时间；
3. 门店名称；
4. 业务编号后四位（若有）。

---

## **11. 门店说明**

### **11.1 说明内容**

门店可对节点或图片填写说明，例如：

```
左前翼子板存在凹陷，已进行钣金修复。
刹车片磨损接近极限，已更换新件。
完工后已进行路试检查。

```

### **11.2 展示规则**

1. 仅展示审核通过或系统可信来源说明；
2. 不展示辱骂、广告、联系方式引流；
3. 不展示用户隐私；
4. 不展示未经确认的新增收费描述。

---

## **12. 隐私提示**

服务相册页面必须展示隐私提示：

```
服务相册仅订单本人可见。未经你授权，平台不会将原始相册公开展示。
如你选择公开为案例，平台会先进行隐私脱敏和内容审核。

```

---

## **13. 公开授权入口**

### **13.1 入口定位**

订单完成后，可在服务相册或服务相册页展示：

```
授权生成公开案例

```

或：

```
申请公开为案例

```

### **13.2 展示条件**

必须满足：

1. 订单已完成；
2. 相册图片数量达到最低要求；
3. 无未处理售后争议；
4. 用户为订单本人；
5. 订单未被风控限制；
6. 门店允许案例公开，或平台规则允许；
7. 不涉及禁止公开内容。

### **13.3 授权说明**

点击授权前展示：

```
你的服务相册可供其他车主参考。公开并通过平台审核后，可按平台规则参与利益共享（以政策为准）。平台会自动脱敏车牌、手机号等信息，请你核对后再确认公开。公开版不展示完整订单信息与实际支付金额。

```

说明区下方勾选文案后附 **《利益共享政策》** 链接（跳转规则页占位，正式政策文档待补齐）。

### **13.4 用户可选择公开范围**

P0 可简化为：

```
同意平台脱敏后公开展示本次维修案例

```

P1 可支持选择：

1. 仅公开车型和服务项目；
2. 公开维修过程图；
3. 公开门店名称；
4. 公开评价内容；
5. 不公开价格；
6. 允许被分享。

### **13.5 授权勾选**

用户必须主动勾选：

```
我已核对公开内容，同意将本次服务相册公开为案例。《利益共享政策》

```

### **13.6 拒绝公开**

用户可选择不公开，页面需提供「拒绝公开」操作（与「确认公开为案例」并列，不使用警告色）。

点击后二次确认：

```
拒绝后，本次服务相册仍仅作为你的私密记录保存，不会生成公开案例。

```

确认后记录 `user_rejected` 状态；相册仍可正常查看，后续可在规则允许时重新发起公开（P1）。

### **13.7 确认公开与脱敏预览**

用户勾选并点击「确认公开为案例」后：

1. 若商家已提交完工且 **`service_pre_mask`（兼容旧称 `order_pre_mask`）** 任务已就绪（`preMaskStatus=ready` / `partial_failed` 且有可用脱敏图），直接进入 `pages/desensitize/preview` 预览工作台，**复用已有脱敏图，零等待、不现场补跑 OCR**；
2. 预览页展示原图 / 脱敏图对比，用户须逐张查看脱敏图；
3. 用户可重新处理失败项；手工打码 P1；**车主授权场景不做「一键脱敏」降级**（脱敏由完工后系统预脱敏完成）；
4. 勾选确认项后点击「确认并公开」，写入授权记录；
5. **预脱敏未就绪**（处理中 / 未生成 / 指纹过期需后台刷新）：**不进入**预览页，提示「配图脱敏处理中，完成后会通知你，请稍后再试」；就绪后发站内消息（及已授权的微信订阅消息）；
6. **预脱敏失败**：提示「配图脱敏未完成，暂无法发布到公开网站」，不开放车主侧现场 OCR。

---

公开前必须脱敏：

1. 车牌；
2. VIN；
3. 手机号；
4. 姓名；
5. 人脸；
6. 身份证；
7. 行驶证；
8. 保险单号；
9. 详细住址；
10. 二维码；
11. 其他可识别个人身份的信息。

### **14.1 图片脱敏**

图片脱敏包括：

1. 遮挡车牌；
2. 遮挡人脸；
3. 遮挡 VIN 铭牌；
4. 遮挡证件；
5. 遮挡门禁/地址等敏感信息；
6. 必要时裁剪图片。

### **14.2 文本脱敏**

文本脱敏包括：

```
张三 → 张**
13812345678 → 138****5678
浙A12345 → 浙A****5
VIN 完整码 → 不展示或仅显示后四位

```

---

## **14. 列表与详情展示口径（Phase 1）**

### **14.1 我的服务相册列表**（UI-ALB · 加高横向卡 · **UI-ALB-G 清档案**）

**Tab**：**全部** | **进行中** | **已完工**（默认 **全部**）。

> **2026-08-13**：废止以案例审通过分档。Tab 以 **相册是否已完工** 为界：未完工 → 进行中；已完工 → 已完工。发布到网站属案例模块，不当分类轴。

**列表壳（UI-ALB-G）**：Tab 下方列表包 **档案目录 panel**（`.surface-album-panel`）— 顶栏「**档案目录**」+ 主色 accent 竖线（`text-album-display`）；panel 内纵向排列 `ui-album-card`（`raised` + 相框缩略图）。页面底 `--color-bg-album` 冷白；**禁止**暖褐 sepia 纸感回退。

| Tab | 包含 | 卡片 Tag | 卡片主操作 |
| --- | --- | --- | --- |
| **全部** | 所有相册 | **维修中** 或 **已完工**（维修进度） | 随状态：分享等；案例发布入口另案 |
| **进行中** | 相册尚未标记完工 | **维修中** | 随状态；**详情始终可打开** |
| **已完工** | 相册已标记完工 | 可弱化维修进度 Tag | 分享等；案例发布另案 |

**卡片布局（A-06c + UI-ALB-G）**：左 **相框缩略图** + 右 **档案信息块**（服务名、门店、车辆·张数、Tag、送修日期）；**无右侧操作列**（方案 C）。点击卡片进详情；授权/分享/撤回/核对配件在 **尾页 + 底栏 + 抽屉**。

**阶段 E · 结构化摘要（U-ALB-11，待开发）**：

| 字段 | 展示位置 | 规则 |
| --- | --- | --- |
| `deliverDateText` | 卡片 meta 首段 | `送修 MM-DD`；取 `createdAt`（无则 `updatedAt`） |
| `summaryLine` | 卡片 meta 第二行 | 见 §14.4；单行 `·` 分隔，超出省略 |
| `metaLine` | 卡片 meta 保留 | 车辆 · 张数（与摘要行分行，不重复车辆） |

一屏通常 **2～4 条**；禁止单条占满视口。

### **14.2 服务相册详情**（UI-ALB · 无置顶摘要 · §7.6.0）

详情页 **不再** 展示置顶摘要卡与双层 Tag 头；维修/可见性 Tag 已在 **列表卡片** 展示。

详情仅保留：

| 区域 | 内容 |
| --- | --- |
| 清单阅读（2026-08-14） | 类目分段 + 检查项卡（父项与衍生合成主题卡）；结果一项一次；图下仅人话说明；点图全屏 |
| 页底操作区 | 「本册已阅」+ 服务名 + **评价** / 联系门店等（无分享、检查与档案、配件验真文字链） |
| 信息抽屉（阶段 E 恢复） | PrivacyBanner + **KeyInfoTable 档案摘要** + 配件清单 + 门店说明列表 |

**已移除（§7.6.0）**：详情 **页脚** `AlbumPageFooter`、主屏 ComplianceNotice（合规归设置/协议与工具页 shell）。

**禁止**对用户展示「可授权」「已授权」「已公开」等内部状态名（列表 Tag 仍用 Phase 1 用户可见文案）。

### **14.3 信息架构分工（A-06b + 阶段 E）**

| 信息/操作 | 列表 | 详情清单 | 页底操作区 |
| --- | --- | --- | --- |
| 标题/服务/门店/车辆/张数 | ✅ | — | — |
| 送修日期 + 结构化摘要行 | ✅ | — | — |
| 状态 Tag | ✅ | — | — |
| **待评价 Tag**（案例审通过且未评） | ✅ | — | — |
| 清单阅读 / 阶段图 | — | ✅ | — |
| 单张全屏看图 | — | ✅ | — |
| 分享 | — | — | —（列表卡等入口；详情页底不放链） |
| 发布到公开网站 / 撤回 | — | — | ✅（有状态时） |
| **检查与档案** | — | — | —（本期详情不暴露） |
| **配件验真** | — | — | —（本期详情不暴露） |
| **服务评价**（完工且未评） | — | — | ✅ 主按钮「评价」 |
| 问题反馈 | — | — | ✅（经评价页弱链） |
| 联系门店 | — | — | ✅ |

> **2026-08-14**：详情页底收束为评价 + 联系门店；下线页底「分享 / 检查与档案 / 配件验真」文字链。  
> **2026-07-31 · 评价参与**：可评价 = **完工 + 平台案例审通过**。入口须 **可发现、可回流**——不得仅依赖页底文字链。第二次打开可评价未评相册时，详情底部可展示可关闭提示条（本地记忆；无现金诱导）。  
> **2026-08-13**：整本全屏翻页下线；主阅读改为清单。  
> **2026-08-02 · REV-MOD**：`pages/album/engage` 已评态展示 **门店回复**；车主可 **追评一次**；评价随案例上网自动公开（见商家套餐 §5.1）。

线框真源：`docs/00_设计规范/11_工具相册UI线框.md` §0 / §2 / §3。

### **14.4 档案字段展示映射（阶段 E · UI-ALB-E-01）**

> **口径**：下列字段对标行业电子维修档案的 **13 项子集**，在 Phase 1 **仅作展示聚合**，不要求商家按监管表单逐项上传；数据来自现有相册字段与六阶段节点，由 **B-ALB-08** 服务端聚合。

#### 列表 `summaryLine` 拼接规则（优先级从左到右，最多 3 段）

| 段 | 来源字段 | 聚合规则 | 缺省 |
| --- | --- | --- | --- |
| 故障/损伤 | `issueDesc` | 接车节点 `note` 首句；或 `vehicle.issueDesc`；或检测节点 `note` 摘要（≤24 字） | 省略该段 |
| 配件摘要 | `partsSummary` | `partsJson` 前 2 项：`名称×数量`；超出加「等 N 项」 | 省略该段 |
| 参考价 | `referencePriceText` | 有 `planAmount` → `参考¥{planAmount}`（PriceDisplay **fixed** 口径）；否则区间价文案 | 省略该段 |

示例：`制动异响 · 刹车片×2 · 参考¥1280`（段间 ` · `，整行超 40 字尾部 `…`）。

#### 抽屉 `KeyInfoTable` 行（`summaryRows` 扩展）

| 标签 | 来源 | 节点/字段映射 |
| --- | --- | --- |
| 送修日期 | `deliverDateText` | `createdAt` 格式 `YYYY-MM-DD` |
| 更新时间 | `updatedAtText` | 列表已有 |
| 服务项目 | `serviceName` | 相册顶字段 |
| 门店 | `store.name` | 相册顶字段 |
| 车辆 | `vehicleDisplay` | `vehicleJson` 格式化 |
| 里程 | `mileageText` | `vehicleJson.mileage` + `km`；无则「—」 |
| 故障/损伤 | `issueDesc` | 接车 `stage_1.note` 或 `vehicle.issueDesc` |
| 检测结果 | `inspectionResult` | 检测 `stage_2.note` 摘要 |
| 维修方案 | `repairSolution` | 方案节点 `stage_3.note` 或 `storeNote` 摘要 |
| 配件摘要 | `partsSummary` | 同列表段 |
| 方案报价 | `planAmount` | PriceDisplay fixed；无则隐藏该行 |
| 质保时长 | `evidenceItems.warranty.duration` | 有则显示 |
| 质保范围 | `evidenceItems.warranty.scope` | 有则显示 |
| 质保说明 | `evidenceItems.warranty.note` | 有则显示；仅有承诺书图时可显示「已上传承诺书」 |
| 图片总数 | `imageCount` | `{n} 张` |

**不在 KeyInfoTable**：授权态、内部 `publicCaseStatus`、脱敏任务 ID。

#### 抽屉配件清单（U-ALB-13）

| 列 | 字段 | 说明 |
| --- | --- | --- |
| 名称 | `partsJson[].name` | 必填 |
| 类型 | `partsJson[].partType` | Tag 展示（原厂/品牌/拆车件等） |
| 数量 | `partsJson[].qty` | 默认 1 |
| 凭证 | 首图 thumb | 可选；点击跳到配件相关节点首张（若可解析） |

空列表：**不展示**配件小节（非 Empty 占位）。

### **14.5 API 契约预览（B-ALB-08）**

列表项与详情在现有字段上 **扩展**（向后兼容）：

```json
{
  "deliverDateText": "送修 06-08",
  "summaryLine": "轮廓破损 · 刹车片×2 · 参考¥1280",
  "summaryRows": [{ "label": "送修日期", "value": "2026-06-08" }],
  "partsSummary": "刹车片×2 等 3 项",
  "parts": [{ "partId": "...", "name": "刹车片", "partType": "brand", "qty": 2 }]
}
```

前端 **U-ALB-11/12/13** 仅消费上述字段；聚合逻辑集中在 backend，小程序不重复拼装业务规则。

### **14.6 相册检查页（U-ALB-INSP · 2026-07-05）**

> **入口**：详情沉浸态底栏 **「检查」** → `pages/album/inspect`（非详情内嵌对比/检查 overlay）。

#### 双 Tab 职责

| Tab | 回答 | 展示 |
| --- | --- | --- |
| **完整性** | 该留痕有没有 | 表：**项目 · 重要度 · 状态(✓/×) · 图**；不写缺项长文 |
| **检查方法** | 怎么对、结果意味什么 | **对照 · 怎么核对 · 一致 · 不一致 · 建议**；缺项结论只在此处 |

页顶一句说明 **重要度**（非「必传/必看」）：见 `COMPLETENESS_TAB_HINT`。

#### 证据数据源

| 类型 | 来源 |
| --- | --- |
| 单据 | `evidenceItems` 分槽（定损/报价/工单/结算） |
| 配件凭证 | `partsJson` |
| 旧件留痕 | `evidenceItems` · `category=old_part`（B-EVID-06） |
| 施工过程 | `nodes[stage_5]` 过程图（与旧件分桶） |

#### AI 检查（B-INSP-01 · 2026-07-27 修订）

| 项 | 规则 |
| --- | --- |
| 触发 | 检查页入口 → `pages/album/inspect-ai`；consent 见 `AI_INSPECTION_CONSENT` |
| 试用 | **仅相册已完工**；成功报告限 **1 次**（失败可重试；排队/分析中不可重复发起） |
| 预脱敏闸门 | **必须**等 `service_pre_mask` 就绪后才读图；未就绪 → 报告状态 `queued`（排队中），**不**把原图送给外部多模态模型、**不**在车主点击时现场跑 OCR |
| 自动续跑 | 预脱敏成功后后台 `flush` 排队任务 → 状态 `running` → 调用 LLM；完成后通知车主查看 |
| 脱敏失败 | 取消排队，报告失败，并通知车主 |
| Vision 输入 | **仅脱敏图**（pre-mask `maskedUrl` / `preMaskedUrl`）；无可用脱敏候选则文字-only LLM |
| 文本 LLM 输入 | 结构化摘要 +（如有）读图说明；**不含原图 URL** |
| 输出 | `overallOpinion` / `comparisons` / `photoAppendix` 等（见 normalize 契约） |
| 免责 | **仅** AI 结果区块展示 `AI_INSPECTION_DISCLAIMER` |
| API | `POST /user/service-albums/:albumId/inspection-advice`（快速返回 `queued` / `running` / `failed`；成功结果异步写入后通知） |
| 存储 | `album_inspection_reports`（`payload.status`: `queued` \| `running` \| `success` \| `failed`） |
| LLM | `INSP_LLM_*` / Vision `INSP_VISION_*`；失败写入 `failed` 报告（**不**兜底规则引擎答案） |
| 通知 | 分析完成 / 失败 → 站内消息 +（有额度时）订阅消息，跳转 `inspect-ai` |

#### 底栏

**反馈** · **配件验真**（有配件时）；与详情尾页能力一致，不在检查 Tab 内重复授权/分享。

---

## **15. 公开案例状态**


| 状态                    | 说明       |
| --------------------- | -------- |
| private               | 私密，仅本人可见 |
| authorization_pending | 待用户授权    |
| user_rejected         | 用户拒绝公开    |
| authorized            | 用户已授权    |
| desensitizing         | 脱敏处理中    |
| review_pending        | 待平台审核    |
| public_approved       | 已公开      |
| public_rejected       | 审核未通过    |
| public_removed        | 已下架      |

### **15.1 用户可见文案映射（Phase 1）**

面向用户的 **可见性** 文案（列表/详情 Tag、提示），与内部 `publicCaseStatus` 对应：

| 用户可见 | 内部态（含） | 列表 Tab |
| --- | --- | --- |
| **案例处理中**（弱提示，不挡相册） | `pending_desensitize`、`pending_review`、`rejected`（列表可提示公开进度；**详情可打开**） | **进行中** |
| **私密相册** | `private`、`authorization_pending`、`user_rejected`；以及已过审未发布的 `review_passed`、撤回后的 `offline` | **进行中**（未过审）或 **已完工**（已过审 / 撤回后） |
| **公开相册** | `public_approved` | **已完工** |

**维修进度**（与可见性、列表 Tab 独立）：卡片上的 **维修中** / **已完工** 由 `album.status` 判定；列表 Tab「已完工」表示 **案例审已通过**，不是维修进度同义词。

> **2026-08-11**：相册与案例文稿分模块。关联车主对服务相册 **全程可打开详情**；案例审只审文稿，不挡看相册。废止「完工后至案例审通过前详情 403」。

撤回后：公开页下线（`offline`）；**仍属已完工 Tab**（案例审已通过，再发免审）；可再次经分享 **发布到公开网站**。

### **15.2 状态提示**

用户拒绝公开：

```
你已拒绝公开，相册仍可作为私密服务相册查看。

```

脱敏处理中：

```
门店已完工，平台正在处理案例配图脱敏；你仍可查看服务相册。

```

待审核：

```
平台正在审核案例文稿；你仍可查看服务相册。通过后可选择是否发布到公开网站。

```

审核通过待发布：

```
公示内容已通过审核，可预览并确认是否发布到公开网站。

```

审核未通过：

```
该案例暂不适合公开展示，你仍可作为私密服务相册查看。

```

---

## **16. 与「发布到公开网站」的关系（V2.0 · 修订 2026-07-18）**

服务相册本身不绑定交易评价或现金奖励。

**发布到公开网站**（非「发给微信 / 复制去社交媒体」）的条件：

1. 车主主动点击 **发布到公开网站**（预览 = 即将上网内容）；
2. 公开素材 **强制脱敏**（PublicView）；
3. **人工审核**通过；
4. **未点发布则不上** H5 / GEO。

通过后展示于 **H5 公开案例页 / 站外 GEO**（**不含** 小程序案例 Tab）。历史用语「授权公示」= 本路径（见口径 §4.5）。

V2.0 **不做**：订单完成评价、评价奖励、分享领钱。可做 **荣誉/心理鼓励**（无现金）。

**口径**：`00_Phase1_服务相册产品口径.md` §4 / §4.5（用户主导发布）。

---

## **17. 分享规则**

> **定稿 2026-07-31**：用户端 **不做「复制去社交媒体 / 复制文案」**（避免功能堆砌）；车主侧分享以 **发给微信** + **发布到公开网站** 为主。商家端案例稿「复制文案」保留。

> **定稿 2026-07-18（卷十二 USER-PUB）**：**发给微信** 与 **发布到公开网站** 分离。发给微信 **不要求** 先上公开网站；公开网站须 **车主点一下 + 人工审核**。私人链路 **仅脱敏图/文**（原图分享已废止，见口径 §4.5.4）。

### **17.1 发给微信与复制社交媒体（不上公开网站）**

| 项 | 规则 |
| --- | --- |
| **谁可以** | 关联车主 |
| **何时** | 相册 **已完工**；**不必** 先发布到公开网站或过审 |
| **发给微信** | 好友、朋友圈（原生能力）+ 复制私人分享链接 |
| **复制去社交媒体** | 小红书 / 知乎 / 头条等长文 + **一键复制**；车主自行粘贴；**商家/平台不代发** |
| **脱敏** | **强制仅脱敏**；完整原图仅小程序内登录态可见 |
| **与公开网站** | **不** 自动进入 H5 / GEO |

私密服务相册 **不可** 把「仅登录可见的相册详情链接」当公开外链；须走分享能力生成的脱敏链接/文案。

### **17.2 发布到公开网站**

前置：**平台案例审已通过**（`review_passed`）。车主在 ShareSheet 选择 **发布到公开网站** → 预览 PublicView（脱敏）→ 确认留痕 → **立即** `public_approved` → H5 / GEO（**不再**发布后再审）。

此路径 **强制脱敏**；与是否发给微信、是否复制到小红书 **无关**。  
**禁止** 未确认即上网；**禁止**「分享即公开」；**禁止** 案例审未通过时开放发布入口。

### **17.3 分享文案限制与荣誉鼓励**

分享卡片 / 按钮 **不得**出现：

```
分享有奖
点击有奖
点赞有奖
浏览有奖
帮我赚钱
转发领钱
```

可使用内容型 / 荣誉向文案：

```
这次维修过程已整理，可发给朋友参考。
帮助同城车主少踩坑（发布到公开网站后）
```

后台可做传播归因；**无** 用户侧现金奖励结算。物质感谢由门店线下自行处理，平台不承诺、不结算。

### **17.4 半屏分享面板（定稿 2026-07-18 · 三列）**

> **目标**：一个「分享」入口；三列意图分清；与口径 §4.5.1 对齐。落地见卷十二 `USER-PUB-U-01`。

#### **17.4.1 为什么需要半屏面板**

| 问题 | 说明 |
| --- | --- |
| 右上角「复制链接」 | 仅能复制小程序链，不能改成 HTTPS 公网地址 |
| 多种意图并存 | 微信私传、社交媒体复制、上公开网站须分开展示，入口宜合并 |

半屏分享 = 点击「分享」后弹出 **ShareSheet**；**不是**新页面路由。

#### **17.4.2 入口与页面布局（相册详情）**

| 区块 | 展示 | 操作 |
| --- | --- | --- |
| **分享** | 已完工 | **一个主按钮「分享」** → ShareSheet 三列 |
| **已在公开网站** | 已 `public_approved` | 状态文案 + 查看公开页；复制公开网页链接在面板内 |

**禁止**：「须先授权才能分享」「分享即公开」；面向用户 **不用**「授权公示」作主按钮文案（改用「发布到公开网站」）。

#### **17.4.3 半屏面板结构（目标态）**

```
┌─────────────────────────────┐
│  分享                    ✕  │
│  [PrivacyBanner]              │
│  ── ① 发给微信 ──             │
│  （完工即可；不会自动上网）     │
│  [ 转发给微信好友 ]            │
│  [ 复制分享链接 ]              │
│  （朋友圈：见右上角说明）       │
│  ── ② 复制去社交媒体 ──       │
│  [ 小红书/知乎/头条/公众号/抖音 ] │
│  长文预览（LLM 按平台语气）+ 复制 │
│  ── ③ 发布到公开网站 ──       │
│  （须预览确认；过审后上 H5）    │
│  [ 预览并发布 ] 或状态文案      │
│  （过审后：[ 复制公开网页链接 ]）│
│  [ 取消 ]                     │
└─────────────────────────────┘
```

**PrivacyBanner**：私人/社交媒体用 `ownerShare`（仅脱敏）；发布列用 `authorize` / 发布说明；过审后公开链接组用 `publicCaseShare`。

#### **17.4.4 渠道规则**

| 渠道 | 可见条件 | 行为 | 结果 |
| --- | --- | --- | --- |
| **转发给微信好友** | `completed` | token 私人分享 | **不上**公开网站 |
| **复制分享链接**（私人） | **同上** | `album/share.html?token=`（**仅脱敏**） | **不上**公开网站 |
| **复制去社交媒体** | `completed` | 多平台长文 + 剪贴板 | 车主自行发第三方；**不上**辙见 H5 |
| **预览并发布到公开网站** | `completed` 且未在审/未过审 | WYSIWYG → 留痕 → 待审 | 过审后上 H5/GEO |
| **复制公开网页链接** | `public_approved` | `case/view.html?id=` | 强制脱敏公开页 |
| **分享到朋友圈** | 私人可用 | 面板说明 + `onShareTimeline` | 不可面板内一键调起 |

##### **17.4.4.1 私人「复制分享链接」**

```text
用户打开面板 → mode 固定 desensitized
  → POST /service-albums/:id/share { mode: 'desensitized', channel }
  → 复制 https://{domain}/album/share.html?token={token}
```

| | **复制分享链接**（私人） | **复制公开网页链接** |
| --- | --- | --- |
| 前置 | `completed` | `public_approved` |
| 是否上公开网站索引 | **否** | **是**（已发布且过审） |
| 脱敏 | **强制脱敏** | **强制脱敏** |
| URL | `/album/share.html?token=` | `/case/view.html?id=` |

#### **17.4.5 与右上角菜单的关系**

| 能力 | 半屏面板 | 右上角 ··· |
| --- | --- | --- |
| 转发好友 | ✅ | ✅ |
| 朋友圈 | 📄 说明 + `onShareTimeline` | ✅ |
| 复制私人公网链接 | ✅ | ❌ |
| 复制公开网页链接 | ✅（须已过审） | ❌ |
| 复制社交媒体长文 | ✅ | ❌ |

#### **17.4.6 留痕与 API**

| 动作 | 留痕 |
| --- | --- |
| 私人 · 转发好友 | `POST …/share` `{ mode: 'desensitized', channel: 'wechat' }` |
| 私人 · 复制分享链接 | `{ channel: 'owner_h5_link' }` |
| 复制社交媒体长文 | `{ channel: 'social_copy', platform }`（卷十二） |
| 发布到公开网站 | 既有 public-case / 授权留痕（文案改发布） |
| 复制公开网页链接 | `{ channel: 'public_h5_link', caseId }` |

#### **17.4.7 状态与禁用**

| 状态 | 面板 |
| --- | --- |
| 分享 token 准备中 | 微信转发/复制链接 disabled |
| 长文生成中 | 社交媒体复制 disabled |
| 审核中 | 发布列展示「审核中」；不可重复提交 |
| 已过审 | 发布列改「已在公开网站」+ 复制公开链接 |
| 未 `completed` | 不展示「分享」入口 |

#### **17.4.8 Phase 范围**

- **已落地（U-CASE-07）**：ShareSheet + 私人 token + 公开链接（旧文案「授权公示」待卷十二替换）
- **卷十二**：三列 IA、社交媒体复制、发布文案与荣誉鼓励（`USER-PUB-*`）

---

## **17.5 配件验真（`pages/album/part-verify` · U-ALB-PV · 2026-06-24 · 口径修订 2026-07-03）**

> **命名**：全站统一 **「配件验真」**（原「核对配件」废弃）。  
> **定位（2026-07-03）**：帮消费者根据 **配件编码自行查询**，核对 **查询结果中的配件类型** 是否与 **相册登记类型** 一致；**不鉴定** 真伪，**不保证** 已装车实物一致。  
> **非车主验真范围**：方案 `planParts` 与相册 `partsJson` 的类型一致由 **商家** 在录入时保证（报价识别出类型则阶段四 **锁定**）；车主页 **不做** 方案↔相册双列对照。

### **17.5.1 对照模型（页顶必展示）**

```text
  ① 维修方案 / 报价单（商家侧留痕 · 车主可选查阅）
  ② 相册登记配件（类型 · 品牌 · 编码 · 凭证图）
           │  车主验真：复制编码 → 自行查询 → 对比登记类型
  ③ 车上实物配件
           │  平台无法监督 → 更换时建议在场
```

### **17.5.2 入口**

| 入口 | 文案 |
| --- | --- |
| 相册详情 · 尾页 | **配件验真** |
| 相册详情 · 底栏「配件」 | 同左 |
| 信息抽屉 · 配件清单下 | **配件验真** + 一行摘要 |
| `pages/album/engage` | 第三项 **配件验真** |

副标题 hint：**「根据编码查询，核对登记类型」**。

### **17.5.3 页面结构（线框见 `11_工具相册UI线框` §5）**

1. **说明区**：价值主张 + `PrivacyBanner scene=partVerify`  
2. **如何验真（醒目）**：复制编码 → 自行查询 → 对比登记类型  
3. **方案参考（可选）**：note / 报价图缩略图，**非**对照主流程  
4. **验真卡（逐件）**：仅 **门店登记** 列 — 名称 · 类型 Tag · 品牌 · **编码** · 凭证图  
5. **结论（单选）**：查询与登记一致 / 有疑问 / 暂未验真  
6. **装车提醒折叠块**  
7. **提交**：勾选 consent + **保存验真记录**

**方案外增项**：单独分组，仅展示登记侧（`source: extra` 且未绑定 `planPartId`）。**Phase 1 正常路径**下增项应已并入阶段三 `planParts`，验真以 **登记卡 + 方案参考** 为主；见 `04/11_施工增项留痕SOP.md`。

### **17.5.4 验真状态（对用户文案）**

| 内部 value | 用户文案 |
| --- | --- |
| `matched` | 查询与登记一致 |
| `question` | 有疑问 |
| `skipped` | 暂未验真 |

### **17.5.4b 商家端 · 阶段四类型锁定（2026-07-03）**

| 场景 | 规则 |
| --- | --- |
| 报价单识别 **含** 配件类型 | 阶段四 **不可改** 类型；尝试修改 → 提示与车主沟通并在方案节点上传 **最新报价单** |
| 识别 **无** 类型 / 手工选类型 | 展示提示：类型须与报价单一致；变更须沟通车主并更新报价单 |
| 阶段四顶部 | 展示 **车主验真指引**（与车主页「如何验真」一致） |

### **17.5.5 API 契约（B-ALB-09 · 目标）**

`GET /user/service-albums/:albumId/part-verifications`

```json
{
  "planSummary": "…",
  "planQuoteThumbs": ["…"],
  "pairs": [
    {
      "planPart": { "name": "…", "partType": "…", "partCode": "…" },
      "albumPart": { "name": "…", "partType": "…", "partCode": "…", "thumbUrl": "…" },
      "linkStatus": "linked",
      "fieldDiffs": ["partType"]
    }
  ],
  "extras": [],
  "summary": { "matched": 1, "question": 0, "pending": 2, "label": "…" },
  "consentText": "…",
  "onsiteReminder": "…"
}
```

`POST` 仍按 `partKey` upsert；有 `planPartId` 时优先 `linkKey` 对齐。

### **17.5.6 降级（无 `planParts` 的旧相册）**

- 页顶黄条：「暂无结构化方案配件目录，仅可按相册登记项验真」。  
- 仍展示阶段三方案摘要（note / summaryRows）+ `partsJson` 列表。  
- **不报错、不阻断** 保存。

### **17.5.7 合规文案（`partVerify`）**

- 平台不鉴定配件真伪；不代替门店告知义务；验真记录仅关联车主私密相册。  
- 编码可复制供车主 **自行查询**；平台不提供鉴定结论。  
- 相册无法证明已装车件；关键更换建议 **在场** 或 **到店复核**。

### **17.5.8 与问题反馈**

「不一致或有疑问」保存后，可 **弱引导** 打开 `pages/album/feedback`（不强制、不替代验真留痕）。

---

## **18. 问题反馈与售后**

### **18.1 反馈入口**

每张图片或每个节点可提供：

```
对此节点有疑问

```

点击后进入反馈表单。

### **18.2 反馈类型**

1. 图片看不清；
2. 维修说明不清楚；
3. 与实际车辆不符；
4. 未看到关键过程；
5. 对维修结果有疑问；
6. 其他。

### **18.3 售后入口**

如果订单已完工或已完成，展示：

```
申请售后

```

跳转订单售后流程。

---

## **19. 空状态**

### **19.1 暂无相册**

```
门店暂未上传服务相册。
如你对维修进度有疑问，可联系门店或平台客服。

```

### **19.2 节点无图片**

```
该节点暂无图片。

```

### **19.3 相册整理中**

```
服务相册正在整理中，请稍后查看。

```

---

## **20. 异常与边界场景**

### **20.1 图片加载失败**

```
图片加载失败，请稍后重试。

```

### **20.2 相册被下架**

```
该服务相册暂不可查看。

```

### **20.3 审核中**

```
部分图片正在审核中，审核通过后将展示。

```

### **20.4 订单售后中**

如订单存在售后争议，公开授权入口可隐藏或置灰：

```
订单售后处理中，暂不可公开为案例。

```

### **20.5 用户撤回授权**

P0：用户在 **任何时候** 均可撤回公开授权（含案例已审核通过并已公示）。

撤回后：

1. 公开案例下架；
2. 已产生的传播数据保留后台记录；
3. 奖励是否撤销按奖励规则处理；
4. 私密服务相册仍保留。

---

## **21. 合规要求**

1. 原始服务相册默认私密；
2. 未经用户授权不得公开；
3. 公开前必须脱敏；
4. 公开前必须审核；
5. 不得展示完整车牌、VIN、手机号、证件；
6. 不得展示用户实际支付金额；
7. 不得把私密相册链接分享给陌生用户；
8. 不得在分享入口展示诱导奖励文案；
9. 事故车案例公开时不得暗示线上最终报价；
10. 涉及保险、事故责任、违法信息时需谨慎审核。

---

## **22. 埋点统计**


| 埋点                              | 说明       |
| ------------------------------- | -------- |
| repair_album_view               | 服务相册页曝光  |
| repair_album_node_view          | 节点曝光     |
| repair_album_image_click        | 点击图片     |
| repair_album_image_preview      | 大图预览     |
| repair_album_feedback_click     | 点击问题反馈   |
| repair_album_public_auth_click  | 点击授权公开   |
| repair_album_public_auth_submit | 提交公开授权   |
| repair_album_share_click        | 点击分享公开案例 |
| repair_album_aftersale_click    | 点击售后     |


---

## **23. 接口建议**

### **23.1 获取服务相册**

```
GET /api/user/orders/{order_id}/repair-album

```

### **23.2 获取节点图片**

```
GET /api/user/repair-albums/{album_id}/nodes/{node_id}/images

```

### **23.3 提交公开授权**

```
POST /api/user/repair-albums/{album_id}/public-authorization

```

### **23.4 查询公开案例状态**

```
GET /api/user/repair-albums/{album_id}/public-case-status

```

### **23.5 图片问题反馈**

```
POST /api/user/repair-albums/{album_id}/feedback

```

---

## **24. 数据结构建议**

### **24.1 相册对象**

```
{
  "album_id": "alb_001",
  "order_id": "ord_001",
  "service_name": "钣喷修复",
  "merchant_name": "杭州某某汽修",
  "vehicle_display": "宝马 3系 / 浙A****8",
  "album_status": "private",
  "image_count": 18,
  "nodes": []
}

```

### **24.2 节点对象**

```
{
  "node_id": "node_001",
  "node_name": "损伤确认",
  "status": "completed",
  "description": "左前翼子板存在凹陷",
  "image_count": 4,
  "updated_at": "2026-05-18 10:00:00"
}

```

### **24.3 图片对象**

```
{
  "image_id": "img_001",
  "thumb_url": "https://cdn.xxx/thumb.jpg",
  "preview_url": "https://cdn.xxx/preview.jpg",
  "description": "施工前损伤照片",
  "uploaded_at": "2026-05-18 10:00:00",
  "node_id": "node_001"
}

```

---

## **25. P0 范围**

P0 必须实现：

1. 用户从订单进入服务相册；
2. 仅订单本人可查看；
3. 按维修节点展示图片；
4. 图片大图预览；
5. 展示上传时间和说明；
6. 展示隐私提示；
7. 展示服务相册入口；
8. 支持订单完成后申请公开授权；
9. 公开前提示脱敏和审核；
10. 私密相册不可直接对外分享；
11. 图片加载失败和空状态处理；
12. 问题反馈/售后入口。

---

## **26. P1 可后续优化**

1. 图片保存水印；
2. 视频相册；
3. 前后对比模式；
4. 用户评论节点；
5. 用户撤回公开授权；
6. 自动脱敏预览；
7. 公开案例编辑预览；
8. 维修报告 PDF；
9. AI 生成维修摘要；
10. 节点订阅提醒。

---

## **27. 验收标准**

1. 用户可从订单详情进入服务相册；
2. 非订单本人不可查看相册；
3. 相册按节点展示；
4. 图片可正常预览；
5. 图片加载失败有提示；
6. 车牌、手机号、VIN 不在公开区域展示；
7. 私密相册不可直接分享给陌生用户；
8. 用户可看到隐私提示；
9. 订单完成后可申请公开授权；
10. 授权前必须提示脱敏和审核；
11. 未授权相册不会公开；
12. 公开案例不展示实际支付金额；
13. 事故车案例不展示线上最终价；
14. 分享入口不出现诱导奖励文案；
15. 用户可对相册问题提交反馈（非平台售后仲裁）。

---

## **28. 相关文档**

1. `01_用户端小程序PRD V2.0.md`
2. `06_咨询记录与我的相册.md`
3. `07_服务相册查看页.md`（本文档）
4. `05_咨询与到店预约.md`
5. `docs/04_维修过程相册/01_维修过程相册PRD V2.0.md`
6. `10_复杂度分级详细标准.md`

