# 商家端 · 事件节点与单据节点链流程（DOC-FLOW）

> **生效**：2026-09-03  
> **修订**：2026-09-06 · **检测报告与方案一次通知车主**（废止单独「送达报告」）；发现项三档点选  
> **状态**：产品定稿 · 一期实现中  
> **废止**：ALB-UX 四 Tab 为主导航；「单据嵌入拍照节点」；独立质保节点；检测报告车主确认；工单车主确认；发现项「现象/症状」；检查结果自由文本；**单独送达检测报告后再发方案**  
> **配套**：[`23_面向车主单据与确认件.md`](./23_面向车主单据与确认件.md) · [`15_案例档案与托管状态机.md`](../01_项目总览与业务架构/15_案例档案与托管状态机.md)

---

## 1. 目标

一次服务 = 一条 **按时间排序的节点链**。节点分两类：

| 类型 | 作用 |
| --- | --- |
| **拍照 / 过程节点** | 收过程照片，并在本步写齐下游单据所需正文（主诉、发现项、质保等） |
| **单据节点** | 承载面向车主的单据；按门禁通知或确认 |

商家编辑页 = **单页竖向时间线**（`flow-stepper`）：已完成折叠只读、当前步展开内嵌编辑、未解锁不展示。

**原则**

- **内容前移**：正文在上游过程步填写并自动保存  
- **逐步解锁**：不是一屏摊齐  
- **报告 + 方案合发**：检测证据与报价对车主一次可见；车主只确认方案  
- **用户可见文案 ≠ 产品说明**（设计体系 §2.4.1）  
- **私密报价 ≠ 公域藏价**：方案确认写真实金额；公域案例仍遵价格合规  

---

## 2. 标准节点链（无增项 · 7 步 · flowVersion 4）

```
接车与检测 → 检测报告 → 方案确认 → 工单 → 施工 → 完工照 → 完工确认
```

| sort | kind | 显示名 | nodeCategory | 车主侧 |
| ---: | --- | --- | --- | --- |
| 0 | `intake_inspection` | 接车与检测 | photo | —（商家确认本步） |
| 1 | `inspection_report` | 检测报告 | document | **与方案同次通知后可阅**（不确认） |
| 2 | `quote_confirm` | 方案确认 | document | **需确认**（真实报价） |
| 3 | `work_order` | 工单 | document | **只读留档**（不确认） |
| 4 | `work` | 施工 | photo | 过程可阅 |
| 5 | `delivery_photos` | 完工照 | photo | —（商家确认本步） |
| 6 | `repair_report` | 完工确认 | document | **需确认** |

内部 `kind` 仍可用 `quote_confirm` / `repair_report`；产品文案分别为「方案确认」「完工确认」。

### 商家「核对报告与方案」（合一步编辑）

接车与检测确认后，时间线当前步为 **检测报告**，但页面为 **一屏**：

1. **上半**：检测报告（主诉 / 发现项 / 可选结论）— 可微调  
2. **下半**：方案项目（由「需关注 / 需处理」发现项预填行名；**金额手填**）  
3. **主按钮「通知车主」**：一次写入  
   - 检测报告 → `delivered`（完成）  
   - 方案确认 → `pending_confirm`（待车主确认）  

**废止**独立「送达车主」（仅报告）按钮。车主在同一次通知后同时看到报告与方案；**仅方案需确认**。

通知后：报告折叠为已完成；当前步变为方案确认（等待确认 / 代确认）。

### 渐进展示

- 仅展示：已完成（一行，可点开只读）+ 当前步（展开）。  
- 解锁：上一步完成（过程 `completed` / 报告+方案已通知 / 方案 `confirmed` / 工单商家开始施工等）→ 下一步 `in_progress`。  
- 已完成不可改；修订 = 新节点（二期）。

### 过程步草稿（`photoDraft`）

| 过程步 | 本步须写清 |
| --- | --- |
| `intake_inspection` | **进店主诉**（客户视角）；每张发现项：**部位**（手填）+ **检查结果**（三档点选）+ **处理建议**（需关注/需处理时必填） |
| `work` | 每张施工图本图说明；可「添加方案」插增项 |
| `delivery_photos` | 交车图本图说明；质保期限 / 范围 / 除外 |

确认过程步：缺必填硬拦截。上传与改文自动落库。

### 视觉（混合）

| 单据 | 风格 |
| --- | --- |
| 检测报告 | **证据风**：发现项大图 + 部位 + 结果档位 + 处理建议 |
| 方案确认 / 工单 / 完工确认 | **单据风**：页眉车辆摘要 + 条目列表 + 合计/确认条 |

---

## 3. 增项（一期）

施工中可插入新的 **方案确认**（`quote_confirm`，`insertedReason=addon`）→ 车主确认 → **新工单** → 回到施工。  

完工确认汇总 **全部** 已确认工单项目与金额。  

二期可加独立「增项检测」拍照节点。

```
施工 ⇄ 方案确认(增项) → 工单 → 施工 → … → 完工照 → 完工确认
```

---

## 4. 单据字段表与生成规则（真源）

> 不用大模型。一期规则生成 + 商家微调。

### 4.1 检测报告 `inspection_report`

| 区块 | 字段 | 规则 |
| --- | --- | --- |
| 页头 | 车辆 / `mileageText` / `chiefComplaint` / `reportDate` | 相册 + photoDraft |
| 发现项 | `findings[]` | 每图：`partName`；`result` ∈ {状态良好, 需关注, 需处理}；`advice`（见校验） |
| 未检 | `disclaimer` | 固定套话 |
| 结论 | `conclusion` | 可选 |

> **分工**：`chiefComplaint` = 客户进店原因；发现项 = 技师按部位点选结果档并写建议。  
> **校验**：部位必填；结果必选三档之一；**需关注 / 需处理** 时处理建议必填；**状态良好** 默认建议「无需处理」（可改）。修/换写在建议里，不写进结果档。  
> **方案预填**：仅从「需关注 / 需处理」且有建议的发现项带入；状态良好不预填报价行。行名建议为「部位 · 建议」。

**生成**：接车与检测确认 → 报告完整草稿；同时写入方案节点预填行（金额空，待填）。  
**门禁（商家）**：在「核对报告与方案」一屏补齐金额后 **通知车主** → 报告 `delivered` + 方案 `pending_confirm`。  
**车主**：报告可阅、**不确认**；与方案同次出现。

### 4.2 方案确认 `quote_confirm`

| 字段 | 说明 |
| --- | --- |
| `lines[]` | `{ name, amount, note, evidenceUrl? }`；`amount` 为 **真实金额（元）** |
| `confirmCopy` | 同意按上述项目施工；费用以本单为准 |
| `evidenceRef` | 关联检测报告节点 id |

预填：发现项 `部位 · 建议` → `name`，`amount` 手填。  
**门禁**：≥1 行且每行 `amount` 为有效数字；与报告一并通知后待确认；车主确认或代确认 → 自动生成工单。

### 4.3 工单 `work_order`

| 字段 | 说明 |
| --- | --- |
| `items[]` | `{ name, amount, note }` 自方案行带入 |
| `sourceQuoteNodeId` | 来源方案确认节点 |

**生成**：方案确认通过后自动生成并展示；车主只读。商家「开始施工」→ 解锁施工步。

### 4.4 完工确认 `repair_report`

| 区块 | 字段 |
| --- | --- |
| 已施工 | 全部工单 `items`（含增项） |
| 合计 | 已确认方案金额合计 |
| 验证 | `deliveryPhotos[]` |
| 质保 | period / scope / exclusions |
| 确认文案 | `confirmCopy` |

**生成**：完工照确认后。车主确认或代确认。

---

## 5. 确认与代确认

**需车主确认**：仅 `quote_confirm`（方案确认）、`repair_report`（完工确认）、增项方案确认。  

**不需**：`inspection_report`（随方案通知后可阅）、`work_order`（只读）。

| 状态 | 展示 |
| --- | --- |
| draft | 草稿 |
| delivered | 已送达（检测报告，随「通知车主」写入） |
| pending_confirm | 待车主确认 |
| confirmed | 车主已确认 / 商家代确认 + 时间；代确认可选证明图 |

---

## 6. 数据模型

`content_package_json.flowVersion = 4`，`flowNodes[]`。

| kind | legacyStageIds |
| --- | --- |
| intake_inspection | stage_2（存量 stage_1 并入） |
| work | stage_5 |
| delivery_photos | stage_6 |

过程节点可带 `photoDraft`。

---

## 7. 门禁

- 过程步必填硬拦截（主诉/发现项/质保）。  
- 「通知车主」：报告完整 + 方案金额硬校验。  
- 整单完工：建议完工确认已确认（软提示可保留）。

---

## 8. 分期

| 期 | 范围 |
| --- | --- |
| 一期 | 7 步链 + **报告与方案合发** + 方案真实报价 + 工单自动 + 完工确认 + 施工中插方案增项 |
| 二期 | 车主异步确认回写完善、增项检测拍照节点、修订指纹 |
| 三期 | 类目清单模板、微信迁入 |

---

## 9. 存量兼容

| 类型 | 策略 |
| --- | --- |
| flowVersion≥4 | 本口径 |
| v3 | 打开时标题/门禁按本口径迁移视图（报告不再要求确认） |
| ALB-UX 四阶段 | 仍可读旧 edit |
| 已单独「送达」报告、方案仍草稿 | 打开后进方案步补金额再发；新单走合发 |
