# 商家端 · 事件节点与单据节点链流程（DOC-FLOW）

> **生效**：2026-09-03  
> **修订**：2026-09-09 · 方案行名仅部位；废止商家「结论」栏；报告与方案合发；车主侧见 `23` §5；商家时间线步号写在各步标题（不写「第 n / 总步数」）；增项未发送取消无痕；已发送作废填原因双端可见；施工一行多图；完工可勾选本单施工图；**2026-09-13** · 接车独立里程；生产短提示；公开托管内容检查；增项多轮公网保留；**2026-09-14** · 确认前检查（不硬拦、后台续跑、能力开关、建议原位贴字段不弹层）；**检测写检查发现、方案写处理建议；占位按服务类目**；**2026-09-15** · 发现项折叠时仍露出该行建议，不自动展开某一项；**2026-09-16** · 检测图是证据、方案/施工按要做的事列行，可多图挂同一行；**当前里程与仪表照同一区，拍仪表识别填数**；**2026-09-24** · 新发现先写故障照片和看见什么，报价不再传图；车主可不同意，增项记为车主未同意并回到施工，首次方案与完工退回门店修改   
> **状态**：产品定稿 · 一期实现中  
> **废止**：ALB-UX 四 Tab 为主导航；「单据嵌入拍照节点」；独立质保节点；检测报告车主确认；工单车主确认；发现项「现象/症状」；检查结果自由文本；**单独送达检测报告后再发方案**；**商家填写整单结论栏**；**车主端以照片流为主阅读**  
> **配套**：[`23_面向车主单据与确认件.md`](./23_面向车主单据与确认件.md) · [`15_案例档案与托管状态机.md`](../01_项目总览与业务架构/15_案例档案与托管状态机.md)

---

## 1. 目标

一次服务 = 一条 **按时间排序的节点链**。节点分两类：

| 类型 | 作用 |
| --- | --- |
| **拍照 / 过程节点** | 收过程照片，并在本步写齐下游单据所需正文（主诉、发现项、质保等） |
| **单据节点** | 承载面向车主的单据；按门禁通知或确认 |

商家编辑页 = **单页竖向时间线**（`flow-stepper`）：已完成折叠只读、当前步展开内嵌编辑、未解锁不展示。  
各步标题带本册序号（如「第1步 · 接车与检测」）；**不**在顶部展示「第 n / 总步数」（总步数随增项变化，易误解为固定总长）。

**原则**

- **内容前移**：正文在上游过程步填写并自动保存  
- **逐步解锁**：不是一屏摊齐  
- **报告 + 方案合发**：检测证据与报价对车主一次可见；车主只确认方案  
- **用户可见文案 ≠ 产品说明**（设计体系 §2.4.1）  
- **私密报价 ≠ 公域藏价**：方案确认写真实金额；公域案例仍遵价格合规  

---

## 2. 标准节点链（无增项 · 7 步 · flowVersion 4）

```
接车与检测 → 检测报告 → 方案确认 → 工单 → 施工 → 完工照 → 完工确认
```

| sort | kind | 显示名 | nodeCategory | 车主侧（见 `23` §5：单据链，不镜像拍照步） |
| ---: | --- | --- | --- | --- |
| 0 | `intake_inspection` | 接车与检测 | photo | 不单独成车主节点 |
| 1 | `inspection_report` | 检测报告 | document | **与方案同次通知后可阅**（不确认） |
| 2 | `quote_confirm` | 方案确认 | document | **需确认**（真实报价） |
| 3 | `work_order` | 工单 | document | **只读留档**（不确认） |
| 4 | `work` | 施工 | photo | 不单独成车主节点；图可挂后续单据 |
| 5 | `delivery_photos` | 完工照 | photo | 不单独成车主节点；交车照进完工确认 |
| 6 | `repair_report` | 完工确认 | document | **需确认** |

**车主时间线** ≠ 上表逐步镜像：车主只见单据链（检测报告 → 方案确认 → 工单 → 完工确认），整链灰显未到步；触达靠线下或分享，**不**依赖相册照片更新订阅（`23` §5.4–5.6）。

内部 `kind` 仍可用 `quote_confirm` / `repair_report`；产品文案分别为「方案确认」「完工确认」。

### 商家「核对报告与方案」（合一步编辑）

接车与检测确认后，时间线当前步为 **检测报告**，但页面为 **一屏**：

1. **上半**：检测报告（主诉 / 发现项）— 可微调  
2. **下半**：方案项目（由「需关注 / 需处理」发现项预填**部位名**；**金额手填**）  
3. **主按钮「通知车主」**：一次写入  
   - 检测报告 → `delivered`（完成）  
   - 方案确认 → `pending_confirm`（待车主确认）  

**废止**独立「送达车主」（仅报告）按钮。车主在同一次通知后同时看到报告与方案；**仅方案需确认**。

通知后：报告折叠为已完成；当前步变为方案确认（等待确认 / 代确认）。

### 渐进展示

- 仅展示：已完成（一行，可点开只读）+ 当前步（展开）。  
- 解锁：上一步完成（过程 `completed` / 报告+方案已通知 / 方案 `confirmed` / 工单商家开始施工等）→ 下一步 `in_progress`。  
- 已完成不可改；修订 = 新节点（二期）。

### 过程步草稿（`photoDraft`）

| 过程步 | 本步须写清 |
| --- | --- |
| `intake_inspection` | **进店主诉**（客户为什么来；**占位按本单服务类目**，钣喷不要出现亏电例句）；**当前里程**（选填）：数字框与 **仪表照同一行**，拍表盘后识别填数、可手改；图作证据，**不**放进下方检测照片、**不**进方案。可改 **对外车型**（品牌 / 车系 / 年款）；每项发现：**部位** + **检查结果**（四档：状态良好 / 需关注 / 需处理 / **仅记录**）+ **检查发现**（需关注/需处理时必填，写看见什么；仅记录不填）。**同一部位可多张照片**，共用这一套结果和检查发现。拍照一句：拍部位、能看清结论，不要拍码。**液位、环车等留证图选「仅记录」**，不当作要修的部位 |
| `work` | **一行多图**（每项最多 6 张）：**项目**（必填，与方案行「要做的事」一致，如全车补漆）+ **说明**（选填，整项一句，写规范动作；不写异常发现——异常归检测）；**无**检查结果、**无**处理建议；可「发现新的待处理项目」，入口旁短句「下一页拍故障、写看见什么」；已有未完成的新发现时再点不会另开一张 |
| `delivery_photos` | **交车证据选用**（非第二套相册）：从本单施工图**引用勾选**；**整车外观必选**（可从施工图选，没有则补拍 1 张；提示拍车身、不要拍码）；其他交车图选填（勾施工图或补拍）；确认前检查若建议补拍，建议旁可直接拍，图进其他交车图。**质保两行**：期限（必填、须写实质，禁「以门店公示为准」）/ 注意事项（选填） |

确认过程步：缺必填硬拦截。上传与改文自动落库。

### 确认前检查（2026-09-14 · 2026-09-26 修订）

店员点「确认并继续」或「通知车主」前，可按**该类服务 × 这一步**给出优化方向。真源见 [`28_`](./28_节点AI检查_用户关心点与开发计划.md)。

| 规则 | 口径 |
| --- | --- |
| **哪些步** | **不在接车与检测时查**（这一步还不通知车主）。通知车主前，把检测报告和报价放在一次里核对：可以按报价补检测所见，也可以按检测补报价的项目或施工方案。查出后两边都能改，检测报告的改法与接车填写相同（照片、部位、检查结果、检查发现）。**施工完成时、完工照完成时各查一次本步**（不拦下一步），把照片与描述写准；这两次可**只读**引用已确认的检测报告与报价方案作参考，用完不改。**完工通知前再查一次**，覆盖施工与交车照；**报价方案发给车主确认前（首次报价与增项同一口径）也查**，避免报价页单独发送时整段不过检查。车主确认或代确认之后，后续检查不再改已经固定的检测和报价。**工单不跑** |
| **硬拦 vs 建议** | 缺必填仍硬拦。检查结果贴在**原字段 / 原照片下方**（不同字色与字号）：文案旁「应用」只改这一块；缺图在该部位下补拍，照片并入该项，不另开一项。对不上已有部位才加一行待拍。报价证据、施工图、交车图同样：对得上就加在该项，对不上才新增。发现项默认折叠、一次只展开一项；**有未处理建议的行在折叠态也露出该条**（与补拍条同一位置），展开后再贴到对应输入框下，避免叠两遍。**不**为了展示建议而自动展开某一项。看完后「进入下一步」（通知车主前则「通知车主」）。**不挡下一步，不弹层** |
| **必须先看到再决定** | 建议未出时本步先不完成；等待时主按钮收起，只留一行橙色「正在检查」+ 进行中图标。关掉小程序后打码与分析继续；再打开仍停在本步，建议仍贴在对应位置 |
| **先打码再分析** | 只用打码图问模型；失败不把原图送出去。打码图与建议挂在该节点，托管不再对同图整单重打 |
| **方案怎么才算写好** | 每条报价行的施工方案要写清「做什么工序、用什么件与规格、依据哪条发现」，车主和以后看公开案例的人一眼能看懂。只能用本单已有的检测发现与当前行内容组织句子，**不许编造没拍到、没提到的工序或读数**；不改金额、不做承诺 |
| **不改什么** | 不改金额、不编项目、不建议合并增项轮次；已发给车主的确认件不经店员点不改写 |
| **能力开关** | 总闸关闭或未开通时，本步直接确认，与做本能力之前一致。前期全员免费，以后可按套餐收费（见 `28_` §4.4） |

界面：建议贴在对应内容下，短句、商家口吻。不弹层、不加黄底产品说明。不出现「提示词 / 自动优化 / 大模型 / 套餐」等实现话术。

**施工多图 / 交车选用（2026-09-09）**

- 施工按「维修项」留证：同一部位可多角度。检测同一部位的补拍也并入该项（多图共用检查结果和检查发现）；只有部位对不上时才新开一项。方案证据、交车图同样。  
- **不**再维护与施工平行的第二套「完工相册」；交车图 = 对施工图（及少量补拍）的**引用**，档案不因勾选而复制文件。  
- **整车外观必选**：有施工图也未必含全貌；无外观时仅补拍外观（或从施工图中指定一张为外观），不默认整本重拍。  
- 过程拆解/旧件特写默认不进车主交车区，除非商家勾为交车图或按检查建议补拍。

### 视觉 · 只读单据壳（一期）

车主服务进度与商家**已完成/只读预览**共用组件 `service-doc-sheet`（纸质单感、小程序单列）：

| 区域 | 内容 |
| --- | --- |
| 单头 | 单类型标题 · 状态 · 店名 |
| 副行 | 日期 · 车辆 · 里程 |
| 正文 | 检测：主诉 +「本次检测项目」；方案/工单/完工：条目表感 + 合计（完工含交车图/质保） |
| 协议 / 确认 | 方案与完工；确认钮仅车主 |

商家**编辑态**（填金额、改发现项）二期再贴同一视觉；一期编辑区保持现表单。

| 单据 | 阅读风格 |
| --- | --- |
| 检测报告 | 证据条目（大图发现项）+ 分区标题「本次检测项目」 |
| 方案 / 工单 / 完工 | 单据条目 + 合计 / 协议条 |

---

## 3. 增项（一期）

施工中入口文案：**发现新的待处理项目**（勿写「添加方案」，易歧义）。旁路一句：**下一页拍故障、写看见什么**。点一次即可；已有未发出或待确认的新发现时，再点只回到这一页，不另开一张。  
公网档案**保留**由此产生的多段方案 / 工单 / 施工，不合并、不按主项裁；第二轮方案标题用「施工中新发现」（见 `15_` §7.9）。  

插入一张 **方案确认**（`quote_confirm`，`insertedReason=addon`）。同一页先写 **新发现**（故障照片 + 看见什么），写好后再填 **项目与费用**。项目与费用与首次报价相同：项目、品牌（选填）、施工方案、金额，**不再上传故障图**（照片在新发现里）。车主确认 → **新工单**（时间线**另开一张**，与首张工单并列，不合并）→ **继续施工拍照**（确认后直达施工步，不必卡在空工单页）→ …  

**单据大标题（商家 + 车主统一）**：**施工中新发现**。时间线段标签可弱标「增项」。  

未发送车主前，方案/增项费用行可 **增删**（至少保留一行空壳便于继续填）。

### 3.1 整张作废（误点入口 / 线下谈妥不做）

- **谁操作**：仅商家。  
- **何时**：增项「施工中新发现」处于未发或等待车主确认；**车主已确认后不可作废**。  
- **未发出（误点）**：直接删掉本张增项及后续未开工单/施工步，**不留时间线记录、不填原因、不通知车主**；当前回到原施工。  
- **已发送待确认**：作废须 **必填原因**；时间线保留该步，状态「门店已取消」+ 原因（**商家与车主均可见**）；撤掉后续未开工单/施工；回到原施工。  
- **车主已确认**：不可作废。  

### 3.2 车主不同意

待确认的方案、施工中新发现、完工确认，车主都可以点 **不同意**。电话联系门店仍保留。

- **施工中新发现**：效果与商家「取消本项」相同（这项不做，回到施工）。记录为 **车主未同意**，原因记「车主拒绝」，不记成门店取消。车主与门店都看得到。  
- **首次方案、完工确认**：整单不删。单据退回门店可改，状态记 **车主未同意**。门店改完可再发。  

**施工图部位**：打断增项前须落库当前施工草稿；各施工步 `photoDraft.findings` 按图 URL 合并读取，避免部位名丢失。新工单项目在施工步预填部位名，待补图。  

**工单合并时机**：时间线保持多张工单；**仅完工确认**汇总全部已确认工单项目与金额（`collectAllWorkOrderItems`）。  

二期可加独立「增项检测」拍照节点。

```
施工 ⇄ 施工中新发现 → 工单(增项·独立) → 施工 → … → 完工照 → 完工确认(合并全部工单项)
```

**验收**：改增项相关代码前后，按 [`27_DOC-FLOW增项最小验收清单.md`](./27_DOC-FLOW增项最小验收清单.md) 点测。

---

## 4. 单据字段表与生成规则（真源）

> 不用大模型。一期规则生成 + 商家微调。

### 4.1 检测报告 `inspection_report`

| 区块 | 字段 | 规则 |
| --- | --- | --- |
| 页头 | 车辆 / `mileageText` / `chiefComplaint` / `reportDate` | 相册车辆档 + 接车步 `photoDraft.mileageKm`（与仪表照同区；识别可填、以手改为准）；**不要**从发现项图注里猜公里数 |
| 发现项 | `findings[]` | **一项一个对象**：`partName`；`images[]`（该部位的照片，可多张）；`result` ∈ {状态良好, 需关注, 需处理, 仅记录}；`advice`。相册里的照片列表由这项生成。提交时直接校验这项，不按照片地址把结果重新配对 |
| 未检 | `disclaimer` | 落库可保留固定套话；**车主端不展示**该长句，发现项列表上方用分区标题「本次检测项目」 |

> **检测 ≠ 方案。** 检测图回答「查了什么、看见什么」；方案/施工回答「准备做什么」。多张检测图（钣喷多处、故障灯+底盘）可以挂到**同一方案行**。里程表挂在当前里程旁，不进检测发现；液位、环车等选 **仅记录**（或状态良好），**不**生成报价行。  
> **方案不再按发现项一对一预填。** 保养等一项服务会包含多个检测项，所以核对页给空的方案行（或保留商家已写内容），由商家自己写要做的事和金额。照片只留在检测发现项上，一项有几张就显示几张，不挂到方案行。检测结果档**只留在检测报告**，不写进方案/工单行名。  
> **校验**：部位必填；结果必选四档之一；**需关注 / 需处理** 时检查发现必填；**状态良好** 默认「无需处理」（可改）；**仅记录** 不要求检查发现、不进方案证据池。修/换写在**方案处理建议**里。  
> **一项的边界**：照片、部位、检查结果、检查发现属于同一项。补拍只往 `images[]` 里加，不新开一项，也不换掉原照片。相册节点图片是这项的投影；旧数据里只有散图、没有发现项时，才按图注归到同名部位，归不上才新开一项。施工同样是一项多图，只是没有检查结果和检查发现。  
> **分工**：`chiefComplaint` = 客户进店原因；发现项 = 技师所见（含留证图）；方案行 = **准备做什么**与金额。占位按 `templateId`。  
> **废止商家填写「结论」**：一期不以整单结论栏替代发现项。  
> **与已废止「现象/症状」栏的关系**：检查发现写技师所见，不是把客户口述再抄一遍。

**生成**：接车与检测确认 → 报告完整草稿；方案节点给空行壳，待商家按「要做的事」填写。照片不复制到方案行。  
**门禁（商家）**：在「核对报告与方案」一屏补齐项目名与金额后 **通知车主** → 报告 `delivered` + 方案 `pending_confirm`。  
**车主**：报告可阅、**不确认**；与方案同次出现。

### 4.2 方案确认 `quote_confirm`

| 字段 | 说明 |
| --- | --- |
| `lines[]` | `{ name, brand?, amount, note }`；`name`=**要做的事**（如全车补漆）；`brand`=**配件品牌**（选填）；`note`=施工方案（车主侧称处理建议）；`amount` 为 **真实金额（元）**。首张方案不挂检测图。`evidenceUrl` / `evidenceUrls` 仅增项故障图沿用 |
| `confirmCopy` | **固定协议文案**（商家不可改）：本人同意按上述项目施工，费用以本单为准。 |
| `evidenceRef` | 关联检测报告节点 id（首张方案）；增项以行内证据图为准 |

不按检测部位自动拆行。商家写项目名和金额。存量已按部位拆好的行可继续改、可删并。  
**门禁**：≥1 行且每行 `amount` 为有效数字；**增项**另要求每行有故障图；与报告一并通知 / 增项发送后待确认；车主确认或代确认 → 自动生成工单。  
**商家编辑**：一行四格——**项目**（要做的事，如更换机油机滤）/ **品牌**（选填）/ **施工方案**（怎么做，如机油标号和更换范围，不要把项目名再写一遍）/ **金额**。不放检测图。**不展示**确认文案；车主确认页仍称处理建议。

### 4.3 工单 `work_order`

| 字段 | 说明 |
| --- | --- |
| `items[]` | `{ id, name, brand?, amount, note }` 自方案行带入（项目名 = 要做的事）；`id` 沿用方案行 id，用于与施工项挂钩 |
| `sourceQuoteNodeId` | 来源方案确认节点 |

**生成**：方案确认通过后自动生成并展示；车主只读。商家「开始施工」→ 解锁施工步。  
**施工项预生成**：施工步按工单 `items[]` 预生成施工项，见 §6.6。  
**增项工单**：与首张工单在时间线**分开展示**；确认增项后自动进入可拍照的施工步。  
**完工确认**：汇总全部工单 `items`（此时才合并金额与项目列表）。

### 4.4 完工确认 `repair_report`

| 区块 | 字段 |
| --- | --- |
| 已施工 | 全部工单 `items`（含增项） |
| 合计 | 已确认方案金额合计 |
| 验证 | `deliveryPhotos[]` |
| 质保 | **期限**（必填、实质内容）+ **注意事项**（选填；范围/除外等合写）；存量 scope/exclusions 读侧拼入注意事项 |
| 确认文案 | **固定协议文案**（商家不可改） |

**生成**：完工照确认后。车主确认或代确认成功后，**自动整单完工**（相册 `completed`）。

---

## 5. 确认与代确认

**需车主确认**：仅 `quote_confirm`（首张「方案确认」/ 增项「施工中新发现」）、`repair_report`（完工确认）。  

**不需**：`inspection_report`（随方案通知后可阅）、`work_order`（只读）。

**确认文案**：平台固定协议句，写入单据 payload；**商家报价/完工编辑页不展示、不可改**；**车主确认页展示**。

### 商家操作两阶段（方案 / 增项 / 完工）

| 阶段 | 单据状态 | 商家底栏 | 状态文案 |
| --- | --- | --- | --- |
| 编辑 | `draft` | **仅**主按钮「发送车主确认」（发送时保存；**不**单独「保存草稿」） | **不标「草稿」**（摘要可空） |
| 待确认 | `pending_confirm` | 提示「等待车主确认」+ 次按钮「代车主确认」 | 待车主确认 |
| 已确认 | `confirmed` | 无操作 / 只读 | 车主已确认 / 商家代确认 |

**商家代确认**：仅在「待确认」阶段出现，入口为次按钮「代车主确认」。点后先弹窗确认前提——**仅限车主当面、电话或微信已同意，但未在手机确认**；确认后再二选一：**附同意截图（聊天记录即可）** / **不留痕，直接确认**（在确认流程内选传，**不**单独占一行「上传证明」按钮）。

| 状态 | 展示 |
| --- | --- |
| draft | （商家面不展示「草稿」标签） |
| delivered | 已送达（检测报告，随「通知车主」写入） |
| pending_confirm | 待车主确认 |
| confirmed | 车主已确认 / 商家代确认 + 时间；代确认可选证明图 |

---

## 6. 数据模型（真源）

`content_package_json.flowVersion = 4`，`flowNodes[]`。

| kind | legacyStageIds |
| --- | --- |
| intake_inspection | stage_2（存量 stage_1 并入） |
| work | stage_5 |
| delivery_photos | stage_6 |

### 6.1 节点对象字段表

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | string | 是 | 节点唯一标识 |
| `kind` | string | 是 | 见 §2 标准链 |
| `nodeCategory` | `photo` / `document` | 是 | 决定该节点带 `photoDraft` 还是 `document` |
| `sortOrder` | number | 是 | **仅用于排序**，不得当业务主键 |
| `title` | string | 是 | 节点标题 |
| `status` | string | 是 | `locked` / `in_progress` / `completed` |
| `photos` | array | 是 | 散图 `{url, imageId, caption}`；**只读投影**，不是业务真源 |
| `note` | string | 是 | 备注 |
| `photoDraft` | object | `photo` 类必填 | 过程步草稿，见 §6.2 |
| `document` | object / null | `document` 类必填 | 单据，见 §4；`photo` 类为 `null` |
| `legacyStageId` / `legacyStageIds` | string / array | 是 | 存量相册阶段映射 |
| `insertedReason` | string | 是 | 增项时为 `addon` |
| `parentNodeId` | string | 是 | 增项父节点 |
| `segmentLabel` | string | 是 | 分段标签 |

**工厂约定（禁止手写字面量创建节点）：**

- `photo` 类节点 → `photoDraft: emptyPhotoDraft()`、`document: null`
- `document` 类节点 → `document: emptyDocument(kind)`、`photoDraft: null`
- 与 `emptyDocument()` 对称，`emptyPhotoDraft()` 必须产出 §6.2 全部字段

### 6.2 `photoDraft` 字段表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `chiefComplaint` | string | 进店主诉 |
| `mileageKm` | string | 里程 |
| `odometerUrl` / `odometerImageId` | string | 仪表照 |
| `vehicleBrand` / `vehicleSeries` / `vehicleYear` | string | 车型 |
| `conclusion` | string | 检测结论 |
| `findings` | array | 发现项，见 §6.3 |
| `warrantyPeriod` / `warrantyNotes` | string | 质保期限与注意事项 |
| `confirmCopy` | string | 确认文案 |
| `selectedDeliveryUrls` | array | 交车选图 |
| `deliveryExteriorUrl` | string | 整车外观 |

### 6.3 发现项 `finding`（一项的边界）

**属于同一发现项的字段必须住在同一个对象里。禁止按数组下标或图片 URL 跨数组配对。**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | **稳定标识**，见下方 id 规则 |
| `partName` | string | 部位 / 施工项目名。**不是身份**，可重名、可改 |
| `images` | array | 该项图片 `{url, imageId}`，最多 6 张；显式 `[]` 表示已删光，不得用 `url` 回填 |
| `url` / `imageId` | string | 首图冗余，供读侧兼容 |
| `result` | string | 检查结果（状态良好 / 需关注 / 需处理） |
| `advice` | string | 检查发现 |
| `quoteLineId` | string | 关联的方案行 / 工单项 `id`（施工项，可空） |

### 6.4 `id` 规则（真源）

1. **新建** finding 必须分配 `id`。
2. **读取**：`normalize*` 保留 `raw.id`；缺失时按下述顺序生成，且生成后**立即持久化**（页面有自动保存，存量数据打开一次即完成迁移）。
   - 首图指纹：`fid_<媒体键>` —— 同一张图多次 normalize 得到同一 id
   - 仍取不到：`fid_i<下标>` —— **仅存量首次迁移命中**
3. `id` 落库后不随顺序调整、改名、换图而变化。删除项后其 `id` 不复用。

### 6.5 配对规则

- **一律按 `id` 配对**。禁止用数组下标、展示顺序、图片 URL 反查，把 `photos[]` 与 `findings[]` 对齐成一条记录。多图挂在 finding 的 `images[]` 上。
- **禁止用 `partName` 当主键**做合并或匹配（部位可重名）。同名不同 `id` 视为两项。
- 施工项与方案行 / 工单项的对应关系走 `quoteLineId`；**仅在** `quoteLineId` 缺失时才可按名称归一化回落（存量兼容）。
- 散图与 finding 的关联只认 `imageId` / `url`。位置兜底仅允许在存量「一图一项」且两侧数量相等时启用一次，生成 `id` 后不再命中。

**存量兼容**：旧形状在 normalize 里只读不写回；新写入必须是本节结构。

### 6.6 施工项由工单项目预生成

1. 进入施工步时，按**本段工单**（时间线上本施工步之前最近的一张 `work_order`）的 `items[]` **预生成**施工项骨架：每项 `{ id, quoteLineId: item.id, partName: item.name, images: [] }`。
2. 商家只往对应项里传图，**不再手填项目名**；已传的图按 `quoteLineId` 归位。
3. 已有 findings 与新骨架的合并：按 `quoteLineId` 匹配；存量无 `quoteLineId` 的按名称归一化回落**一次**，匹配成功后写入 `quoteLineId`。
4. 匹配不上本工单任何项目的施工项**保留在末尾不丢**（前序施工步已传的项、存量或异常数据）；门禁只认本工单项目。
5. 展示顺序：本工单项目在前（未传图标 `pendingPhoto`），前序施工步遗留项在后。

**多工单并存**：增项工单与首张工单在时间线**并列、不合并**（§3、§4.3）；**每个施工步只认本段工单**，仅**完工确认**汇总全部工单 `items`（`collectAllWorkOrderItems`）。

**增项流程**（施工中新增项目）——不在施工节点自由添加，回到方案侧闭环：

1. 商家点「发现新的待处理项目」→ 插入 `quote_confirm`（`insertedReason=addon`）
2. 车主确认或商家代确认
3. 自动**新生成一张增项工单**（与首张工单**并列、不合并**），并插入一个新的施工步（带入前序已传 findings）
4. 新施工步按**新工单** `items[]` 预生成施工项；前序已传的图按 `quoteLineId` / 名称归位，未匹配到的保留不丢

---

## 7. 门禁

- 过程步必填硬拦截（主诉/发现项/质保）。  
- 「通知车主」：报告完整 + 方案金额硬校验。  
- 整单完工：完工确认（车主确认或商家代确认）成功后**自动**将相册标为 `completed`（通知、预脱敏等）；不再要求商家二次点「标记整单完工」。存量若步骤已齐但相册未完工，商家端可保留一次补点入口。
- **案例托管**：相册 `completed` 后，flow 页底栏出现「托管到案例站」；编辑页只读态同入口。主路径为**原始档案托管**（私密 / 公开二选一），废止「本地生成优化案例稿再推送」。公开须隐私校验 → **内容检查（软建议）** → 生成 GEO → 门店确认 GEO 后才上店页（见 `15_` §7.11 · `19_`）。缺里程不拦完工，公开前警告。

---

## 8. 分期

| 期 | 范围 |
| --- | --- |
| 一期 | 7 步链 + **报告与方案合发** + 方案真实报价 + 工单自动 + 完工确认 + 施工中插方案增项 |
| 二期 | 车主异步确认回写完善、增项检测拍照节点、修订指纹 |
| 三期 | 类目清单模板、微信迁入 |

---

## 9. 存量兼容

| 类型 | 策略 |
| --- | --- |
| flowVersion≥4 | 本口径 |
| v3 | 打开时标题/门禁按本口径迁移视图（报告不再要求确认） |
| ALB-UX 四阶段 | 仍可读旧 edit |
| 已单独「送达」报告、方案仍草稿 | 打开后进方案步补金额再发；新单走合发 |
