# 服务类目检测清单 · 上线方案

| 项 | 内容 |
| --- | --- |
| 状态 | **施工清单 / 跟进清单 IA 定稿**（2026-08-12）；融合迭代已落地，待联调 |
| 日期 | 2026-08-10 |
| 内容真源 | [`17_服务类目检测清单.md`](./17_服务类目检测清单.md)（检查项 **已定稿**） |
| 产品哲学 | [`00_Phase1_服务相册产品口径.md`](./00_Phase1_服务相册产品口径.md) **§2.1** |
| 开发进度 | [`00_开发计划.md`](../00_开发计划.md) **§7.16 卷十五** |

---

## 1. 一句话目标

商家在**四阶段节点内**完成检查与留证；检测结果自动生成**施工清单**（无价格）；车主阅读**完整检查项 + 结果**，同一项下挂旧况与施工图；**不因检测未完成阻断完工**，但施工清单中未施工项须先删除确认。

---

## 2. 已确认决策

### 2.0 首轮（2026-08-10）

| # | 决策 |
| --- | --- |
| 1 | 数据采用 `albums.checklistJson` + `album_images.checklistItemKey`；跨阶段挂同一 `itemKey` |
| 2 | 车主默认清单阅读；**有图/说明才展示该项**（不是空指引目录） |
| 3 | **P0 一次灌全并启用全部类目** |
| 4 | **删除**原相册教练主路径；阶段 `photoTips` 退出主引导 |

### 2.1 融合与施工清单（2026-08-10 确认 · 2026-08-12 IA 定稿）

| # | 决策 |
| --- | --- |
| 5 | **检查清单融入四阶段**：进入接车/检测即列出本阶段检查项；每项多图 + 每图说明；**禁止**「顶部总清单点选 → 下方节点再拍」双路径 |
| 6 | **施工清单**由检测结果**自动生成**（不必写价格）；正常项不进列表；支持手动增删；**不再叫「待处理项目」** |
| 7 | **删除交互（两步）**：点「删除」→ 确认「本次不做」→ 再问「是否记入跟进」；完工时若仍有未施工项，须逐项走同一流程后才能继续；正常施工留证过程不打扰 |
| 8 | **车主**看完整检查项与结果；施工图挂在**同一检查项**下（旧况 + 新件）；正常项仅展示检查图，不要求施工图 |
| 9 | **下线**原厂/副厂等配件类型录入；**下线**报价单及报价 OCR（方案/配件独立节点已取消，遗留 UI/能力一并收口） |
| 10 | **下线完工结算单**：商家/车主相册正文均不展示上传与历史结算单；旧图仅后台存档；**质保**保留 |
| 11 | **节点可见性**：施工节点**仅**施工清单（无跟进区）；完工节点 = 交付项 +（若有）跟进薄入口；跟进项可「重新纳入施工」 |
| 12 | **案例**：只写本单做成的服务；`skipped` 不进案例正文；`follow_up` 最多一句总述，不逐条点名 |
| 13 | **下线「完整性自检」**：不再单独展示节点缺项矩阵与完工缺项弹窗；指引由阶段内清单 + 施工清单关卡承担 |
| 14 | **跟进为经营备忘**：完工页默认薄入口（不占过程正文篇幅），点开再看明细；工作台待办汇总「待回访跟进」 |
| 15 | **编辑页车辆信息**：默认简化（车牌 / 车型 / 车架号），可展开完整编辑 |
| 16 | **质保交互**：与其它项一致——承诺书图 + **每图说明**；三栏（时长/范围/说明）合并为**一段总说明**；车主与案例只展示这一段；仍强烈建议、不阻断完工 |
| 17 | **相册藏价**：商家相册不再提供金额填写；车主/商家相册正文新旧一律不展示金额；旧「方案配件清单」残留入口与行价钱一并收口隐藏；四节点按项目挂的图文不动 |

---

## 3. 跨阶段如何「对上号」（白话）

### 3.1 核心：一份项，多阶段多张图

```text
检测：拍旧刹车片 → 挂 pad_thickness → 结果「建议更换」
施工：施工清单自动出现「刹车片」→ 拍新片仍挂 pad_thickness
车主：同一「刹车片」下看到旧况图 + 新件图 + 结果文案
```

### 3.2 检查结果 → 是否进施工清单

**原则：施工是检测的延伸。**  
无检测（无图无标注）→ 无施工；检测正常 → 无施工；检测有问题 → 必有施工。  
施工清单的进队、标签与状态**必须随当前检测结论同步**：图注/快捷标签能明确推断结果时，**以当前图注为准覆盖落库旧 outcome**（例如先标「建议更换」再改「正常」→ 立刻退出施工清单，不得残留「建议更换 / 待施工」）。  
施工清单里若已出现某项，商家上传施工图后**不得**因尚未点标签（空图注）而从列表消失；离开清单须走删除两步（§3.3）。

| outcome | 含义 | 进施工清单？ | 车主（有图/说明时） |
| --- | --- | --- | --- |
| `normal` | **正常** | 否 | 展示检查图 +「正常」 |
| `observed` | 仅检查（未判好坏） | 默认否（可手动加入） | 展示检查记录 |
| `recommend_replace` | 建议更换 | **是**（见下「队列展示」） | 展示建议 |
| `repaired_other` | 需处理 / 已处理（非更换） | **是**（见下） | 按说明展示 |
| `replaced` | 已更换 | **是**（挂新件图） | 旧况 + 新件 |
| `not_replaced` | 建议换但本次未换 | **是**（或已在跟进） | 按 disposition 展示 |

**施工清单展示（与进队区分）：**

| 类型 | 示例 | 是否出现在施工清单 |
| --- | --- | --- |
| 有衍生项的检测父项 | 旧机油状态 | **否**（只解锁子项；父项留在检测节点） |
| 施工衍生项 | 新机油规格 / 机油滤芯 / 新机油液位 | **是**（仅当父项已有留证且标了需处理/需更换，或手增） |
| 无衍生的异常检测项 | 火花塞（同 key 挂施工图） | **是**（检测异常后本项进队） |

衍生项**禁止**仅因自身已有图、而父项未检/未判异常就进入施工清单。

接车建档项（catalog `group=接车建档`，如里程表、到店诉求）：默认**不**因「有说明 / 自由图注」自动进施工清单；仅当图注或结果明确为建议更换/需处理（及已更换/未更换），或**手动加入**时进入。自由说明（如里程数）不得推断为「需处理」。

### 3.3 删除：本次不做 → 是否跟进

商家点「删除」（或完工关卡触发）后：

1. 确认 **本次不做**（施工清单立刻可移除）  
2. 再问 **需要记入跟进、方便回访再约吗？**

| 原因码 | 含义 | 商家后续 | 车主相册 | 案例 |
| --- | --- | --- | --- | --- |
| `skipped` | 本次不做、**不**跟进 | 离开施工清单；不进跟进 | 保留检测图与结果；**不**强调「本次不做 / 暂缓」 | **不写入**正文 |
| `follow_up` | 本次不做、**需**跟进 | 离开施工清单；进完工「跟进清单」 | 展示该项 +「择日再约」及可选说明 | 正文不逐条点名；最多一句总述 |

兼容读旧值：`mismatch` → 按 `skipped`；`owner_declined` → 按 `follow_up`。

可手动**新增**施工清单项（从已有检查项中勾选未自动进入者）。跟进清单支持「重新纳入施工」。

### 3.4 指引有、但店里没拍

全程无图无 note → **车主不出现该项**（与首轮一致）。

---

## 4. 视角分清

| | **阶段内指引（商家）** | **施工清单（商家）** | **检查阅读（车主）** | **跟进备忘（商家）** |
| --- | --- | --- | --- | --- |
| 出现位置 | 接车 / 检测 / 完工交付 | **仅施工节点** | 详情默认 | **完工薄入口** + **工作台待办** |
| 内容 | 本阶段建议检查/交付的全量项 | 检测延伸的需处理项 | 有证据的检查项 + 结果 + 各阶段图 | `follow_up` 择日再约项（经营备忘，非本单过程正文） |
| 目的 | 少漏检 | 对症施工留证 | 看得懂查了什么、怎么处理 | 回访/再约，不丢单；不抢过程记录篇幅 |

---

## 5. 范围与非范围

### 5.1 做

| 域 | 内容 |
| --- | --- |
| 配置 / 运行时 | catalog；`checklistJson`（outcome + 施工 disposition）；图片 `checklistItemKey` |
| 商家 | 阶段内嵌清单；施工清单自动生成 + 删除两步 + 完工跟进；每项多图多说明 |
| 车主 | 完整有证检查项；同项挂施工图；跟进项择日说明 |
| 案例 | 本单做成的写入；skipped 不写；follow_up 最多总述一句 |
| 下线 | 教练主路径；配件类型录入；报价 OCR；完工结算单（含历史正文隐藏）；相册金额填写与展示 |

### 5.2 不做

| 不做 | 原因 |
| --- | --- |
| 因**检测未完成**阻断完工 | 已拍板；施工未消化项须先删除确认（§2.1 #7） |
| 施工清单写价格 / 报价 OS | §2.1；无价格 |
| 智检红黄绿 / OBD | §2.1 |
| 大店 ERP 同步 | 验证期不做 |

---

## 6. 数据方案

### 6.1 配置

同前：`service-checklist-catalog`。  
- `suggestStageId`：`stage_1` / `stage_2` / `stage_6` → 出现在对应节点清单；`stage_5` → **仅施工衍生项**（`workOnly`，不常驻固定施工清单）。  
- `workFollowUpKeys`：父检查项进入施工清单后，自动解锁子项（例：`old_oil` → `engine_oil` / `oil_filter` / `oil_level_confirm`）。

### 6.2 运行时 `albums.checklistJson`

```text
{
  catalogVersion,
  categoryId,
  items: [
    {
      itemKey,
      status: "pending" | "active" | "skipped",
      outcome: "normal" | "observed" | "recommend_replace" | "replaced"
               | "not_replaced" | "repaired_other" | null,
      note: "",
      imageIds: [],
      work: {
        // 施工清单控制（缺省按 outcome 自动推断）
        source: "auto" | "manual_add" | null,
        removedAs: null | "skipped" | "follow_up"
                   // 兼容旧：mismatch→skipped；owner_declined→follow_up
        deferNote: ""   // follow_up 时车主可见回访说明
      }
    }
  ]
}
```

- 进施工清单：`removedAs` 为空，且（有留证且 outcome ∈ 建议更换/需处理/已更换/未更换 **或** `source=manual_add`），建档类默认排除见 §3.2。  
- 跟进清单：`removedAs=follow_up`（及旧 `owner_declined`）。  
- `removedAs=skipped`（及旧 `mismatch`）：不进施工清单、不进跟进。  
- 车主有证项：有图或 note；跟进项展示择日说明；skipped 不强调「不做」。

### 6.3 与 evidence / 配件报价分离

- 不复用 `evidenceItemsJson` 做检查项。  
- **不再**依赖报价单 OCR、配件原厂/副厂类型作为主路径。  
- 完工质保保留（图 + 每图说明 + 一段总说明）；**下线**结算单 evidence 槽（正文不展示，旧图仅存档）。
- **相册藏价**：不引导填写金额，正文不展示金额。

---

## 7. 端体验摘要

### 7.1 商家

- **接车 / 检测 / 完工**：各节点列出本阶段检查/交付项 → 项内多图 + 图下标签；**取消**项级大文本框。  
- **检测**标「建议更换 / 需处理」等（有留证）→ 触发施工延伸；有 `workFollowUpKeys` 时**只解锁衍生项**进施工清单，父项（如旧机油）不出现在施工列表；无衍生时该项本身进清单。  
- **施工**：**仅施工清单**（解锁衍生 + 无衍生异常项 + 手增）；无「跟进服务」区；上传施工图不掉队；删除走两步。  
- **完工**：交付项 +（若有）跟进薄入口；可重新纳入施工；**不**展示施工清单；**无**结算单槽；质保为图文 + 一段总说明；**无**金额填写；**不做**配件三列表。  
- **点完工**：施工清单仍有未施工项 → 逐项强制删除确认，取消则中断；不因检测项未拍完阻断。  
- 顶部不再放独立总清单主操作区。

### 7.2 车主（阅读 · 修订 2026-08-13）

- 默认：完整有证检查项 + 结果；同项下旧况与施工图一并展示。  
- **一项一卡**：以「服务项目」为阅读单位——先定项目内的 **检查项**，再定其对应的 **施工项**；凡通过 `workFollowUpKeys` 相连（含多个检查项共用同一批施工项）的项合成 **一张卡**；卡内按子项分段。无衍生关系的独立检查项仍各自一张卡。  
- **阶段分组**：卡内图按接车 / 检测 / 施工 / 完工分组，用醒目阶段标题；图下备注 **不再** 重复节点名。  
- **点图**：单张全屏预览，再点退出；检查项内 **不** 再进系统多图翻页。整本「翻页详看」入口可另保留。  
- **质保**：清单下方独立「质保承诺」块（图 + 每图说明 + 一段总说明）；无内容则不出现。  
- **理想配对（引导，非硬拦）**：每个服务项目内，检测留证图与施工结果图宜成对出现（便于车主对比）；本阶段不因缺配对阻断完工或隐藏卡片。  
- `follow_up`：展示「择日再约」及可选说明。  
- `skipped`：只保留检测事实，不强调「本次不做」。  
- 无证据项不出现。  
- 老相册无 checklist → 仍走翻图。

#### 7.2.1 服务项目 = 检查项 + 施工项（车主合成卡真源）

配置真源：`backend/src/constants/service-checklist-work-followups.js`（与 `17` 衍生项一致）。  
合成规则：检查父项 ↔ 其施工衍生项；**共用同一批施工项的多个检查项**并入同一项目卡。

| 类目 | 服务项目（卡标题参考） | 检查项 | 施工项 |
| --- | --- | --- | --- |
| 小保养 / 大保养 | 机油机滤 | 旧机油状态 | 新机油规格、机油滤芯、新机油液位检查 |
| 小保养 / 大保养 | 刹车油 | 刹车油液位 | 刹车油更换/深度检 |
| 小保养 / 大保养 | 防冻液 | 防冻液液位 | 防冻液更换/深度检 |
| 刹车 | 制动检修 | 刹车片厚度、刹车盘厚度、刹车盘盘面状态 | EPB、新配件、新旧对比、力矩/防松、排气说明 |
| 电瓶 | 电瓶更换 | 电瓶检测读数 | 规格匹配、新电瓶、新旧对比、安装固定、匹配/编程 |
| 轮胎 | 轮胎更换 | 花纹/磨损、胎侧/鼓包裂纹 | DOT、规格、新胎、新旧对比、气门嘴、TPMS、拆装动平衡、胎压、轮毂力矩 |
| 空调 | 空调服务 | 滤芯状态、压力/检漏、处理路径（配置上可连通） | 新滤芯、滤芯对比、清洗、冷媒相关 |
| 钣喷 | 钣喷修复 | 修复路径、钣金变形判断 | 漆与辅料、打磨腻子中涂、遮蔽喷涂、卡扣饰条 |
| 事故车 | 事故维修 | 维修范围、损伤清单 | 配件与旧件、钣金校正、涂装、ADAS 标定 |
| 底盘异响 | 底盘检修 | 胶套/球头、连杆、轴承、减震、处理路径 | 配件信息、压装力矩、旧件留影 |
| 通用 | 本次作业 | 检测发现、作业范围 | 关键配件/材料、关键工序 |

无上表衍生关系的检查项（如环车预检、灯光等）→ **各一张卡**。

### 7.3 案例文档

- **已施工 / 有施工留证**：写入案例（旧况 + 处理 + 结果）。  
- **`skipped`**：案例正文不写。  
- **`follow_up`**：不逐条点名；最多一句总述「另有建议项已与车主约定择日处理」。

---

## 8. 分期任务（与 §7.16 对齐）

### 已完成（P0 字段闭环）

CHK-P0-01～08：见开发计划（字段、全类目、初版商家/车主、停用教练）。

### 本轮（CHK-FUSE · 融合迭代）

| ID | 内容 |
| --- | --- |
| CHK-FUSE-01 | 文档：本方案 §2.1～§7 与开发计划任务 |
| CHK-FUSE-02 | outcome 增加 `normal`；施工/跟进 disposition API |
| CHK-FUSE-03 | 商家：检查项融入接车/检测节点（项内多图多说明） |
| CHK-FUSE-04 | 商家：施工清单 + 删除两步 + 完工跟进 |
| CHK-FUSE-05 | 车主：完整有证项 + 同项挂施工图 + 跟进择日说明 |
| CHK-FUSE-06 | 下线配件类型录入、报价 OCR；结算单改建议、保留质保 |
| CHK-FUSE-07 | 施工/完工 IA：施工无跟进、完工无施工清单；案例过滤规则 |

### 原 P1 / P2（顺延）

CHK-P1-01～03、CHK-P2-01～02 见开发计划。

---

## 9. 风险

| 风险 | 对策 |
| --- | --- |
| 只检不标结果 → 施工清单为空 | 完工软提示「有图未标结果」；不阻断 |
| 未施工直接完工 | 强制逐项删除两步；取消则中断完工 |
| 车主以为未查项都没做 | 文案强调「有留证才展示」；正常项显式「正常」 |
| 案例堆未做项显不专业 | 案例只写做成的；follow_up 最多总述一句 |
| 清配件类型影响存量 | 存量只读兼容；新建/编辑主路径不再录入 |

---

## 10. 变更记录

| 日期 | 说明 |
| --- | --- |
| 2026-08-10 | 首版上线方案 |
| 2026-08-10 | **决策确认**：跨阶段同 key + 未更换打标；车主=真实作业清单；P0 全类目；删除教练 |
| 2026-08-10 | **P0 实现落地**：backend + 初版商家/车主路径 |
| 2026-08-10 | **图注交互**：项级大文本取消；图下快捷标签（提醒色）+ 分号补充；自定义；除正常外进待处理；列表简洁/展开 |
| 2026-08-11 | **四阶段拆分落地**：catalog `suggestStageId`→stage_1/2/5/6；施工/完工展示本阶段清单；机油链路修订；材料总览/旧件总表不下做 |
| 2026-08-11 | **施工延伸**：施工不常驻固定清单；`workFollowUpKeys` 由检测进队解锁衍生项（机油等） |
| 2026-08-11 | 刹车油/防冻液：检测仅液位；更换/深度检改为待处理衍生项 |
| 2026-08-11 | **施工队列纠偏**：父项不进施工；衍生项严格靠父项解锁；上传不因未标标签消失；`observed` 不进队 |
| 2026-08-11 | **施工态文案**：待处理列表用「待施工/已施工/未更换」，不用「未检查」；无检测留证不得因残留结果进队 |
| 2026-08-11 | **移出原因交互**：列表不常驻「误判/车主暂不处理」双按钮；仅「移出」或完工核对未施工项时弹原因 |
| 2026-08-12 | **施工清单 IA 定稿**：命名施工清单；删除=本次不做→是否跟进（`skipped`/`follow_up`）；跟进仅完工；案例分流 |
| 2026-08-12 | **下线完整性自检**：商家编辑页移除自检面板/完工缺项弹窗 |
| 2026-08-12 | **跟进薄入口 + 车辆信息简繁**：跟进为经营备忘；工作台待办；编辑顶栏默认简化 |
| 2026-08-12 | **完工收口**：下线结算单正文；质保改图文+一段说明；相册藏价（含旧单） |
| 2026-08-13 | **施工随检测**：图注可推断时覆盖旧 outcome；建档项不因自由说明进队 |
| 2026-08-13 | **车主阅读**：父子项合成卡；图按阶段分组；质保独立块；点图单张全屏 |
| 2026-08-13 | **一项一卡**：按检查+施工连通族合成服务项目；§7.2.1 对照表；检测/施工图成对为理想引导 |
