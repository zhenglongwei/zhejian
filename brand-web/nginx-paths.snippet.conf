# 追加到 simplewin.cn 的 HTTPS server 内（站点根改为 brand-web）
# 不要改 geo.simplewin.cn 案例站

# root /var/www/zhejian/brand-web;
# index index.html;

location = /zhejian {
    try_files /zhejian.html =404;
}
location = /check {
    try_files /check.html =404;
}
